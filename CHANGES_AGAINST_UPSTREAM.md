# Changes against upstream

The authoritative ledger of every in-place edit to an upstream microsoft/vscode file. A reviewer
should be able to read this and know the entire fork surface area. New files in new directories are
NOT listed here (they never conflict); only edits to files that exist upstream.

Base: microsoft/vscode `1.125.0` (see `UPSTREAM_VERSION`).

## In-place edits

| File | Reason | Marker | Upstream area |
|---|---|---|---|
| `README.md` | Merge resolution at Phase 0 import: kept upstream README for the vanilla baseline. Clawdius rebrand lands in Phase 1. | n/a (merge resolution) | root docs |
| `.gitignore` | Appended a marked Clawdius block (research workspace, provider secrets, local config). | `# CLAWDIUS-BEGIN gitignore additions` | root |
| `.github/workflows/*` | Removed the 16 inherited microsoft/vscode CI workflows (they need Microsoft secrets, self-hosted runners, and the distro repo, and fail on any fork); replaced with `clawdius-ci.yml`. | n/a (upstream file removal) | CI |
| `product.json` | M1 branding + privacy overlay: names, app/data/bundle ids, fresh win32 GUIDs, URLs to the repo, extensionsGallery to Open VSX, enableTelemetry false, voiceWsUrl dropped, defaultChatAgent.extensionId off GitHub.copilot, GitHub auth trust emptied. M1 review fix: emptied the five defaultChatAgent Copilot **egress** URLs (entitlementUrl, tokenEntitlementUrl, mcpRegistryDataUrl, managedSettingsUrl, entitlementSignupLimitedUrl) that core `DefaultAccountProviderContribution` fetched at BlockStartup, and repointed the aka.ms/github-copilot plan/signup links to the brand domain. Theme follow-up: curated `onboardingThemes` (the welcomeOnboarding picker list) from the upstream Dark/Light 2026 + Solarized + HC set down to the four Clawdius themes. Phase 2 (B): `defaultChatAgent.provider.default.name` `"GitHub"` → `"Clawdius"` — the central knob that flips ~20 interpolated `{0}` chat-setup strings and the default agent name to "Clawdius Copilot" (paired with the `chatListRenderer.ts` `COPILOT_USERNAME` edit above). Phase 2 (chat backend): added `extensionEnabledApiProposals` allowlisting `vscode.clawdius-chat` for the `defaultChatParticipant` + `chatParticipantAdditions` + `chatProvider` proposals (required once the product is BUILT/`isBuilt`; a dev run enables all proposals); `defaultChatAgent.extensionId`/`chatExtensionId` point at `vscode.clawdius-chat` (the built-in chat extension uses the `vscode` publisher, matching the other built-ins). Data-only. | n/a (data overlay) | product config |
| `src/vs/workbench/contrib/chat/common/languageModels.ts` | Phase 2 (chat backend): `COPILOT_VENDOR_ID` value `'copilot'` → `'clawdius'`. This constant is the "privileged default language-model vendor" used across the chat stack (default-model selection in `extHostLanguageModels.getDefaultLanguageModel`, the vendor descriptor `isDefault`, BYOK detection, model-picker display). Copilot is eliminated, so the `'copilot'` vendor never exists; repointing it at Clawdius's own vendor (the built-in clawdius-chat extension registers a Claude language-model provider with vendor `"clawdius"`) makes Claude the default model so the chat resolves a model instead of throwing "Language model unavailable". Constant NAME kept to minimize merge surface; only the value changes. | `// CLAWDIUS-BEGIN default vendor` | chat / language models |
| `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` | Phase 2 (chat backend): default the `chat.extensionUnification.enabled` setting to `false` (was `true`). Copilot's completions+chat "unification" is irrelevant in Clawdius (Copilot is deleted) and actively harmful: `ExtensionEnablementService._isDisabledByUnification` force-disables the extension whose id equals `product.defaultChatAgent.extensionId` — i.e. our built-in `vscode.clawdius-chat` (the Claude CLI backend) — dropping it from the extension registry so it NEVER activates (silently, no error). Defaulting OFF lets clawdius-chat be the live default chat extension. A product.json `configurationDefaults` override does NOT work here: the enablement service reads this setting before product.json defaults are applied, so the registered default must change. Also (chat parity): registers the new `ClaudeUsageStatusEntry` workbench contribution (import + `registerWorkbenchContribution2`) next to `ChatStatusBarEntry`. | `// CLAWDIUS-BEGIN no copilot unification`, `// CLAWDIUS` (usage entry import + registration) | chat / extension enablement |
| `src/vs/workbench/contrib/chat/browser/chatStatus/media/chatStatus.css` | Phase 2 (chat parity): appended `.clawdius-usage-*` styles for the Claude Code usage tooltip - the capacity bars (reusing `.quota-bar`/`.quota-bit`), the orange contribution heatmap, and the stats grid - styled to match Claude Code's `/usage` view. Also the status-bar content styles (the Claude sunburst mark + per-window capacity pips). | `/* CLAWDIUS-BEGIN claude usage entry */` | chat / status bar |
| `src/vs/workbench/contrib/editTelemetry/browser/editStats/aiStatsStatusBar.ts` | Phase 2 (chat parity): early-return the `AiStatsStatusBar` constructor when `defaultChatAgent.entitlementUrl` is empty (Clawdius), suppressing VS Code's own "AI Usage Statistics" / "Inline suggestions" status-bar entry so it does not duplicate the dedicated Claude Code usage entry. | `// CLAWDIUS-BEGIN no vscode AI-stats entry` | edit telemetry / status bar |
| `src/vs/workbench/services/chat/common/chatEntitlementService.ts` | Phase 2 (sign-in removal): the CHOKE POINT. `ChatEntitlementService` init bails when `defaultChatAgent.entitlementUrl` is empty (Clawdius), leaving `context`/`requests` undefined so `ChatSetupContribution` short-circuits and NONE of the Copilot sign-in/setup/growth chrome registers - the titlebar Sign In pill + toggle, the accounts-menu entry, the F1 setup commands, the upgrade/manage/hide items, the editor-context chat actions, the growth-session nudge, the setup agents, and the setup dialog. | `// CLAWDIUS-BEGIN no copilot setup chrome` | chat / entitlement |
| `src/vs/workbench/contrib/chat/browser/chatStatus/chatStatusEntry.ts` | Phase 2 (sign-in removal): inject `IProductService` and early-return the `ChatStatusBarEntry` constructor when `entitlementUrl` is empty, removing the status-bar "Sign In" entry (whose `CHAT_SETUP_ACTION_ID` command no longer exists). `runningSessionsCount` gets a `= 0` initializer since the early return can skip its constructor assignment. | `// CLAWDIUS-BEGIN no copilot status entry` | chat / status bar |
| `src/vs/sessions/browser/accountTitleBarState.ts` | Phase 2 (sign-in removal): `getCopilotPresentation` returns `undefined` when `entitlementUrl` is empty (static `product` import), so the sessions-window "Agents Signed Out" / "Copilot Unavailable" chip never renders. | `// CLAWDIUS-BEGIN no copilot signed-out chip` | sessions / account |
| `src/vs/workbench/contrib/welcomeOnboarding/browser/onboardingVariationA.ts` | Phase 2 (sign-in removal): drop the onboarding Sign-In step from the step sequence + suppress the "Sign in to use GitHub Copilot" footer nudge when `entitlementUrl` is empty. | `// CLAWDIUS-BEGIN no copilot sign-in onboarding` | welcome onboarding |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts` | Phase 2 (G2, Agents window): the session-source provider's user-visible display name `"Copilot Chat"` -> `"Claude Code"` and icon `Codicon.copilot` -> `Codicon.claude`. The provider id (`COPILOT_PROVIDER_ID`), the nls key, the class name, and all command ids are left intact so the Claude/CLI wiring + tests still bind. Phase 2 (G3): the `sessionTypes` getter omits `CopilotCloudSessionType` (the GitHub Copilot cloud coding agent) when `entitlementUrl` is empty, so a new session in the Agents window does not default to a dead Cloud type that shows "No models available" - the agent-host Claude agent is the sole/default new-session type. | `// CLAWDIUS-BEGIN claude branding (agents window session source)`, `// CLAWDIUS-BEGIN no copilot cloud session type` | sessions / agents window |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessions.contribution.ts` | Phase 2 (G2): two Settings descriptions rebranded - "Copilot Chat sessions provider" -> "Claude Code sessions provider", and the Claude-agent enable setting drops the false "Uses your existing Copilot subscription" clause (Clawdius has no Copilot subscription). Setting keys unchanged. | `// CLAWDIUS-BEGIN claude branding (setting description)`, `// CLAWDIUS-BEGIN claude branding (drop false copilot-subscription claim)` | sessions / agents window |
| `src/vs/sessions/contrib/accountMenu/browser/account.contribution.ts` | Phase 2 (G2): account-avatar alt text `"GitHub profile image for {0}"` -> `"Profile image for {0}"` (screen-reader string). | `// CLAWDIUS-BEGIN no github branding` | sessions / account |
| `src/vs/sessions/contrib/providers/agentHost/browser/openSessionEventsFileActions.ts` | Phase 2 (G2): command-palette title `"Open Copilot CLI State File"` -> `"Open Agent Session State File"`. Command id unchanged. | `// CLAWDIUS-BEGIN no copilot branding` | sessions / agents window |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/permissionPicker.ts`, `.../mobilePermissionPicker.ts` | Phase 2 (G2): the shared permission-picker subtext (nls key `permissions.default.subtext`) `"Copilot uses your configured settings"` -> `"Claude uses your configured settings"`, changed identically in both the desktop and mobile pickers so the shared key stays single-valued. | `// CLAWDIUS-BEGIN claude branding` | sessions / agents window |
| `src/vs/workbench/contrib/chat/browser/agentSessions/agentSessions.ts` | Phase 2 (G2): the Agents-window Claude provider description dropped the misleading "the Claude models included in your GitHub Copilot subscription" and now reads "Delegate tasks to Claude. ...". Architecture-neutral pending the model-transport decision. | `// CLAWDIUS-BEGIN claude branding (no copilot subscription)` | chat / agents window |
| `src/vs/sessions/browser/sessionsSetUpService.ts` | Phase 2 (G2, sign-in removal): `SessionsSetUpWidget._start()` early-returns `onCompleted()` when `entitlementUrl` is empty, suppressing the GitHub/Copilot-branded sessions welcome + sign-in dialog (there is no IDE account to sign into in Clawdius). Mirrors the `accountTitleBarState.ts` choke point. | `// CLAWDIUS-BEGIN no copilot setup/sign-in flow` | sessions / agents window |
| `src/vs/platform/agentHost/node/claude/claudeAgent.ts` | Phase 2 (G3, agent-host Claude goes Copilot-free): in Clawdius mode (empty `entitlementUrl`) `getProtectedResources()` returns `[]` (no GitHub auth advertised), `_ensureAuthenticated()` returns `undefined` instead of throwing `AHP_AUTH_REQUIRED` (no proxy handle needed - the SDK subprocess authenticates via native `~/.claude` OAuth), and `_refreshModels()` publishes a static Claude catalog (`opus`/`sonnet`/`haiku` family aliases + effort schema) instead of querying the Copilot CAPI. The catalog is published from the constructor since `authenticate()` never runs without a Copilot resource. Upstream/Copilot path unchanged when `entitlementUrl` is present. | `// CLAWDIUS-BEGIN native ~/.claude auth`, `// CLAWDIUS-BEGIN static claude catalog` | agent host / claude |
| `src/vs/platform/agentHost/node/claude/claudeSdkOptions.ts` | Phase 2 (G3): `buildOptions(proxyHandle?)` is now optional; when undefined (Clawdius native mode) it omits the `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` CAPI-proxy overrides and `buildSubprocessEnv(false)` stops stripping `ANTHROPIC_API_KEY`, so the SDK subprocess inherits the env and resolves `~/.claude/.credentials.json` OAuth exactly like the `claude` CLI. Handle-present (Copilot) output is byte-identical. | `// CLAWDIUS-BEGIN native ~/.claude auth` | agent host / claude |
| `src/vs/platform/agentHost/node/claude/claudeAgentSession.ts` | Phase 2 (G3): widen `IMaterializeContext.proxyHandle` to optional so a Clawdius session materializes with no CAPI proxy. | `// CLAWDIUS-BEGIN native ~/.claude auth` | agent host / claude |
| `src/vs/platform/agentHost/node/agentHostMain.ts` | Phase 2 (G3): gate `CopilotAgent` (the `copilotcli` provider) registration on a non-empty `entitlementUrl` so it is NOT registered in Clawdius - the dead GitHub Copilot CLI agent only cluttered the Agents window picker and spammed "No token resolved" auth attempts. Leaves the agent-host Claude agent as the sole, default agent. | `// CLAWDIUS-BEGIN no copilot agent in the agents window` | agent host |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts` | Phase 2 (G4, Ultracode P1): `_isCopilotCliAvailable()` returns false in Clawdius so the residual "Copilot CLI" new-session type (a phantom entry with the Copilot label + icon; there is no EH Copilot CLI harness) is never offered. | `// CLAWDIUS-BEGIN no copilot cli session type` | sessions / ultracode |
| `src/vs/workbench/contrib/chat/browser/agentSessions/agentHost/agentHostSessionListController.ts`, `.../agentHostSessionHandler.ts` | Phase 2 (G4): native Claude session rows + the dynamic agent themeIcon + forked-session items rendered the Copilot logo; derive the icon from the session type (`...claude` -> `Codicon.claude`, else `Codicon.copilot`) so Clawdius shows the Claude logo. | `// CLAWDIUS-BEGIN claude icon on native session rows`, `... themeicon`, `... forked native session` | chat / agents window |
| `src/vs/sessions/contrib/providers/localChatSessions/browser/localChatSessions.contribution.ts` | Phase 2 (G4): suppress the Local (VS Code in-process) sessions provider in Clawdius. Its `getModels` returned every user-selectable model with no `targetChatSessionType`, surfacing the clawdius-chat extension vendor as a duplicate "Clawdius" model group alongside the native "Claude" group; suppressing it leaves the agent-host Claude as the sole group (and the Ultracode window native-Claude-only). | `// CLAWDIUS-BEGIN native-claude-only sessions (dedupe model picker)` | sessions / ultracode |
| `src/vs/platform/agentHost/node/claude/claudeAgent.ts` (catalog) | Phase 2 (G4): bump opus + sonnet `maxContextWindow` to 1_000_000 (the opus 4.6+/sonnet 4.6 aliases are 1M-context by default on the native SDK path); haiku stays 200K. | `// CLAWDIUS-BEGIN static claude catalog` | agent host / claude |
| `src/vs/workbench/contrib/chat/browser/widget/input/chatModelPicker.ts` | Phase 2 (G4): show the clickable effort + context-size pills (the "Ultracode effort" selector) in Clawdius. Upstream gates them on usage-based-billing; Clawdius (empty `entitlementUrl`) is always PRU, so `showConfigPills` also enables them there (click handlers are wired unconditionally; the value otherwise only appeared as a label suffix). Upstream PRU unchanged. | `// CLAWDIUS-BEGIN show effort/tokens pills in clawdius (PRU) mode` | chat / model picker |
| `extensions/clawdius-chat/src/extension.ts` (panel models) | Phase 2 (G4): the panel chat model picker showed only the synthetic "Auto" because the LM provider never fired a change event, so the core never eagerly resolved its static models. Added `onDidChangeLanguageModelChatInformation` + fire it once after registration -> core resolves opus/sonnet/haiku into the renderer cache. | n/a (extension; ships only in Clawdius) | clawdius-chat extension |
| `src/vs/workbench/contrib/chat/common/plugins/agentPluginServiceImpl.ts` (new `ClaudeCliAgentPluginDiscovery`) + `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` (registration) | Phase 2 (G5, Ultracode P2): discover plugins installed by the local Claude Code CLI under `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/` so a user's CLI plugins (e.g. Rutherford) appear in the Ultracode Customizations panels - with their bundled skills/agents/commands/hooks + `.mcp.json` MCP server, which `detectPluginFormat` (already returns CLAUDE_FORMAT for `.claude` paths), the base `_toPlugin`, and `PluginMcpDiscovery` wire up automatically. Resolves the active version from `installed_plugins.json` `installPath` (not a glob, since multiple versions coexist) and respects `settings.json` `enabledPlugins`. READ-ONLY mirror (no `remove`). Registered in place of `CopilotCliAgentPluginDiscovery` when `entitlementUrl` is empty. | `// CLAWDIUS-BEGIN claude cli plugin discovery` | chat / plugins |
| `src/vs/sessions/electron-browser/parts/titlebarPart.ts`, `src/vs/sessions/common/categories.ts`, `src/vs/workbench/contrib/chat/electron-browser/agentSessions/agentSessionsActions.ts` | Phase 2 (G4, Ultracode rename): the user-visible "Agents" window name -> "Ultracode" at the window title, the command-palette category, and the Open/Open-in Ultracode (Window) action titles + titlebar hover. Internal ids/keys/context-keys/CSS/the `isSessionsWindow` concept + the Customizations "Agents" (=Claude sub-agents) section are unchanged. | `// CLAWDIUS-BEGIN ultracode window name` | sessions / ultracode |
| `src/vs/platform/agentHost/common/agentHost.config.contribution.ts` | Phase 2 (G3): default `chat.agentHost.enabled` ON for Clawdius (so the agent host spawns in dev AND stable builds) - flipped in-place because this key is read in the main process where product.json `configurationDefaults` are not applied. | `// CLAWDIUS-BEGIN agent host on for clawdius` | agent host / config |
| `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` | Phase 2 (G3): default `chat.agents.claude.preferAgentHost` and `chat.editor.claude.preferAgentHost` to `true` in Clawdius so the agent-host Claude provider surfaces (and the EH provider stands down). Upstream keeps `default: false`. | `// CLAWDIUS-BEGIN prefer agent-host claude` | chat / config |
| `src/vs/platform/agentHost/test/node/claudeAgent.test.ts`, `.../claudeAgent.integrationTest.ts` | Phase 2 (G3): give the test product service a non-empty `entitlementUrl` so the existing tests keep exercising the upstream Copilot/CAPI path (it gates the new Clawdius branch); add a `productService` override hook to `createTestContext` and a dedicated "Clawdius native mode" suite asserting `getProtectedResources()` is empty, the static catalog publishes at construction, and `createSession` runs with no auth gate. Also registers `IProductService` in the integration-test service collections (it was missing). | `// CLAWDIUS-BEGIN copilot-path product service`, `// CLAWDIUS-BEGIN clawdius native-mode coverage` | agent host / tests |
| `src/vs/workbench/services/themes/common/workbenchThemeService.ts` | M1 Phase 4: default color theme constants set to Clawdius Dark/Light; and the pre-theme first-paint `COLOR_THEME_*_INITIAL_COLORS` accent hexes recolored from Microsoft blue (#0078D4/#005FB8) to the Clawdius orange family, so first paint is Clawdius not blue (neutrals + semantic add/delete/error colors untouched; chat-surface accents left for Phase 2). Welcome-picker follow-up: `COLOR_THEME_HC_DARK`/`COLOR_THEME_HC_LIGHT` repointed from upstream `Default High Contrast`/`Default High Contrast Light` to the new `Clawdius High Contrast`/`Clawdius High Contrast Light` themes (folded into the same marked region). | `// CLAWDIUS-BEGIN default theme`, `// CLAWDIUS-BEGIN initial-colors accent recolor` | theme service |
| `build/hygiene.ts` | M1: inverted the upstream "product.json must not contain extensionsGallery" guard into "Open VSX only" (exact `https://open-vsx.org/` host prefix across serviceUrl/itemUrl/resourceUrlTemplate) so the baked-in Open VSX gallery passes hygiene while the Microsoft Marketplace stays blocked. Phase 2 (G1): `checkCopilotEnginesVersion` short-circuits to `undefined` when `extensions/copilot/package.json` is absent (it was deleted), instead of crashing the precommit hook with ENOENT on the missing manifest. | `// CLAWDIUS-BEGIN gallery policy`, `// CLAWDIUS-BEGIN copilot eliminated` | build hygiene |
| `build/npm/dirs.ts` | Phase 2 (G1) fallout: dropped `'extensions/copilot'` from the npm install-target list. `postinstall.ts` runs `npm install` in each listed dir with `cwd` set to it; after the extension was deleted, the missing `cwd` made Node throw `spawn /bin/sh ENOENT`, failing `npm ci` (and thus every CI run) at the postinstall step. This was the lone straggler from the wholesale deletion - `build/hygiene.ts` and `build/lib/extensions.ts` were already guarded. | `// CLAWDIUS-BEGIN no copilot built-in extension` | build / npm install |
| `src/vs/workbench/services/accounts/browser/defaultAccount.ts` | M1 review fix: `DefaultAccountProviderContribution` (registered at `WorkbenchPhase.BlockStartup`) skips registering the GitHub/Copilot default-account provider when `defaultChatAgent.entitlementUrl` is empty (Clawdius empties it), closing ALL startup account egress at the source — incl. the **enterprise** branch that reconstructs `api.{ghe-host}/copilot_internal/*` from user settings, which emptying the product URLs alone did not catch. `DefaultAccountService` opens its init barrier in that no-provider case so `getDefaultAccount()/refresh()/signIn()/signOut()` resolve to "no account" instead of hanging forever (provider registration is otherwise the only `initBarrier.open()`). | `// CLAWDIUS-BEGIN no default-account egress`, `// CLAWDIUS-BEGIN no default-account` | account service |
| `src/vs/workbench/contrib/welcomeGettingStarted/common/media/theme_picker.ts`, `theme_picker_small.ts` | Welcome "Pick a Color Theme" walkthrough: rebranded the four tile labels (Dark Modern / Light Modern / Dark High Contrast / Light High Contrast) to Clawdius Dark / Clawdius Light / Clawdius High Contrast / Clawdius High Contrast Light. The tiles already *applied* the correct themes via the `ThemeSettingDefaults.COLOR_THEME_*` constants; this fixes the visible labels. Markers wrap the module outside the HTML template literal (JS comments cannot live inside it). | `// CLAWDIUS-BEGIN theme-picker labels` | welcome / getting-started |
| `src/vs/workbench/contrib/welcomeGettingStarted/common/media/dark.png`, `light.png`, `dark-hc.png`, `light-hc.png` | Regenerated the four welcome-picker preview thumbnails (874×600) as recolored mockups built from each Clawdius theme's real editor/sidebar/token/accent colors (orange tab indicator + accent pill; the HC variants keep the white-on-black / black-on-white high-contrast frame and divider). Replaces the upstream Modern/HC preview art. | n/a (binary asset) | welcome / getting-started |
| `extensions/theme-defaults/package.json` | "Only Clawdius themes" (Phase 4 follow-up): removed the 10 upstream color themes (Dark/Light Modern, Dark/Light+, VS Dark/Light, Dark/Light 2026, Default High Contrast + Light) from `contributes.themes`; kept the `vs-minimal` icon theme. The inert `themes/*.json` files are left untouched (so they merge cleanly) but are no longer contributed. | n/a (JSON, no inline comments) | built-in themes |
| `extensions/theme-{abyss,kimbie-dark,monokai,monokai-dimmed,quietlight,red,solarized-dark,solarized-light,tomorrow-night-blue}/**` | "Only Clawdius themes": deleted the 9 standalone upstream color-theme extensions wholesale (each contributed exactly one color theme and nothing else), so only the four Clawdius themes ship. `theme-seti` (default file icons) and `theme-defaults` (icon theme) are kept. `branding-guard.ts` scans every extension manifest and fails if any non-Clawdius color theme reappears (e.g. via an upstream merge). | n/a (upstream dir removal) | built-in themes |
| `build/gulpfile.extensions.ts` | Phase 2 (A): registered the new in-tree `extensions/clawdius-chat` extension in the hardcoded `compilations` list so its TypeScript compiles (the auto-glob is commented out upstream, so a new extension is not picked up automatically). | `// CLAWDIUS-BEGIN clawdius-chat` | build / extensions |
| `src/vs/workbench/contrib/chat/browser/widget/chatListRenderer.ts` | Phase 2 (B): `COPILOT_USERNAME` `'GitHub Copilot'` → `'Clawdius Copilot'`, in lockstep with `product.json` `provider.default.name` → `'Clawdius'` (the default agent's display name is `${provider.default.name} Copilot`), so the redundant chat username + avatar stay hidden in the transcript. Must move together with the product.json flip. | `// CLAWDIUS-BEGIN brand username` | chat |
| `extensions/copilot/**` | Phase 2 (G1): "Copilot eliminated". Deleted the GitHub Copilot Chat extension wholesale (the prebuilt `dist/` bundle + 4154 tracked files). It registered SIX competing `isDefault` panel chat participants (`github.copilot.default/editingSession/editsAgent/...`) + 9 `chatViewsWelcome` sign-in overlays on `onStartupFinished`; since the panel default is "last isDefault extension agent wins" (`_preferExtensionAgent`), it raced the Clawdius participant for the panel nondeterministically. With it gone, `clawdius-chat` deterministically owns the panel. Build-safe: core references `github.copilot.*` only as product.json strings + context-key names (no TS import — core type-check stays green), and `packageCopilotExtensionStream` (build/lib/extensions.ts) already guards a missing dir with `fs.existsSync` → returns an empty stream, so the dead `compile-copilot-extension-build` gulp task (still wired into the packaging series in gulpfile.vscode.ts/reh.ts) is a harmless no-op. Those upstream build files are deliberately LEFT UNEDITED to keep the merge surface minimal. `branding-guard.ts` now fails if `extensions/copilot` reappears. | n/a (upstream dir removal) | built-in extensions (Copilot) |
| `eslint.config.js` | Phase 2 (G1) fallout: the root flat-config imported a custom ESLint plugin from INSIDE the deleted extension (`./extensions/copilot/.eslintplugin/index.ts`) and carried ~400 lines of copilot-only lint blocks (7 config objects, lines 2493-2892, using the `copilot-local` plugin). Removed the import + all seven blocks + two now-dead `extensions/copilot/**/*` ignore globs, so the config loads without the missing module (27 blocks remain). Left untouched: the `@github/copilot-sdk` / `@vscode/copilot-api` restricted-import entries and the `copilotChatSessions` sessions-provider zone — those belong to the agentHost subsystem + Agents window, a separate deeper elimination, not this extension. | n/a (block + import removal) | lint config |

## Branding asset replacements (in-place upstream binary / SVG swaps)
The VS Code logo/mark assets, replaced in place with the Clawdius mark (orange `#d97757` family,
re-exported from `clawdius-private-docs/images/clawdius-logo.{svg,png}`). These are upstream files, so a
merge that touches the same asset conflicts; re-export from the Clawdius master rather than hand-merge.
- **OS app-icon family:** `resources/win32/code.ico` + `code_150x150.png` + `code_70x70.png`,
  `resources/darwin/code.icns`, `resources/linux/code.png`, `resources/linux/rpm/code.xpm` (256px),
  `resources/server/code-192.png`, `code-512.png`, `favicon.ico`. `linux/code` + `server/code-192/512`
  share one 1024² master (byte-identical, retina-correct).
- **Inno installer wizard art:** `resources/win32/inno-{big,small}-{100,125,150,175,200,225,250}.bmp`
  (14 BMPs) — Clawdius logo centered on white at each placeholder's footprint size.
- **In-product / chrome logos:** `src/vs/workbench/browser/media/code-icon.svg` (titlebar/banner/welcome),
  `src/vs/sessions/browser/media/vscode-icon.svg` (Open-in-VS-Code), and `sessions-icon.svg`
  (Open-in-Agents widget; blue gradient recolored to the orange family).
- **Auth login chrome (interim, until Phase 2/3 removes the flow):**
  `extensions/github-authentication/media/favicon.ico` + `code-icon.svg`,
  `extensions/microsoft-authentication/media/favicon.ico`.

## New files / directories (no conflict, not part of the diff surface)
`UPSTREAM_VERSION`, `MERGING.md`, `CHANGES_AGAINST_UPSTREAM.md`, `BUILD.md`, `.gitleaks.toml`,
`.pre-commit-config.yaml`, `script/clawdius/**`, `clawdius/**`, `.github/workflows/clawdius-ci.yml`,
`extensions/clawdius-themes/**`, `extensions/clawdius-chat/**` (Phase 2 default chat participant, publisher `vscode`, backed by the local Claude Code CLI; id `vscode.clawdius-chat`),
`src/vs/workbench/services/accounts/test/browser/defaultAccount.test.ts` (Clawdius regression test),
`src/vs/workbench/contrib/chat/browser/chatStatus/claudeUsageEntry.ts` (the Claude Code usage status-bar entry; new file in an upstream dir, registered from `chat.shared.contribution.ts`).

## Intentional egress (not "uninitiated")
- The Claude Code usage pane shows live rate-limit "capacity" bars. The clawdius-chat extension (node, no
  CORS - the renderer can't reach the API) fetches `GET https://api.anthropic.com/api/oauth/usage` every
  ~60s using the user's existing CLI OAuth token (`~/.claude/.credentials.json`) and caches it to
  `~/.claude/.clawdius-usage-cache.json`; the core status entry reads that cache. This is egress to Claude's
  OWN API for the user's OWN usage, and only because the user asked for live capacity bars - it is intended,
  not the "uninitiated egress" the zero-egress guarantee is about. If it should be opt-out, gate the fetch
  behind a setting.

## Known privacy follow-ups (tracked, deferred by design — not yet addressed)
Surfaced by the M1 review; recorded so the zero-egress claim stays honest about what is and is not done.
- `product.json` `webviewContentExternalBaseUrlTemplate` still points at `*.vscode-cdn.net`. Verified this
  is **not** a desktop egress vector: desktop webviews use the local `vscode-webview://{{uuid}}` scheme
  (`environmentService.ts` electron-browser); `vscode-cdn.net` appears only in the web/server build CSP
  (`webClientServer.ts`) and the remote-resource URI transform. Re-point to a Clawdius-controlled host
  before shipping the web/REH build.
- `product.json` `defaultChatAgent` still carries Copilot-specific command/output identifiers
  (`chatExtensionOutputId`, `chatExtensionOutputExtensionStateCommand`, the `github.copilot.*` commands /
  contexts / settings) and a GitHub auth provider block. These are non-URL identifier strings with no
  egress sink. The startup contribution that *consumes* the provider block (`DefaultAccountProviderContribution`)
  is now disabled while `entitlementUrl` is empty (see the `defaultAccount.ts` edit above), so the provider
  data is inert in M1. Replace the identifiers and define the real auth model with the Phase 2 Clawdius chat
  extension (M2).
- `product.json` documentation/terms/privacy/plan/signup URLs are placeholders on `https://clawdiuscode.io`.
  Confirm the domain is registered with landing content before public release.

## Discipline
Every future in-place edit is wrapped in `// CLAWDIUS-BEGIN <reason>` / `// CLAWDIUS-END` (or the
comment syntax of the file), kept minimal, and added to the table above with the file, reason, and
upstream area touched. Prefer data (`product.json`) and new files over in-place edits.
