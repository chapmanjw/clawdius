# Changes against upstream

The authoritative record of every in-place edit to an upstream microsoft/vscode file. A reviewer
should be able to read this and know the entire fork surface area. New files in new directories are
NOT listed here (they never conflict); only edits to files that exist upstream.

Base: microsoft/vscode `1.126.0` (see `UPSTREAM_VERSION`). Rebased from `1.125.0`; see the
"Rebase onto VS Code 1.126.0" section at the end.

## In-place edits

> Reconciliation note (post-pivot): rows 35-37 below describe early Copilot-gating edits that were later SUPERSEDED. `CopilotAgent` was removed from the agent host entirely (no longer referenced in `agentHostMain.ts` or `agentHostServerMain.ts`), and the settings-picker provider edit was rewritten by the native redesign (see rows 50, 69, 71). Their cited `// CLAWDIUS-BEGIN no copilot ...` markers no longer exist in the tree. Where a table row conflicts with a later chronological section, the in-tree `// CLAWDIUS-BEGIN` markers and the chronological sections are the source of truth.

| File | Reason | Marker | Upstream area |
|---|---|---|---|
| `README.md` | Merge resolution at import: kept upstream README for the vanilla baseline. Clawdius rebrand lands later. | n/a (merge resolution) | root docs |
| `.gitignore` | Appended a marked Clawdius block (research workspace, provider secrets, local config). | `# CLAWDIUS-BEGIN gitignore additions` | root |
| `.github/workflows/*` | Removed the 16 inherited microsoft/vscode CI workflows (they need Microsoft secrets, self-hosted runners, and the distro repo, and fail on any fork); replaced with `clawdius-ci.yml`. | n/a (upstream file removal) | CI |
| `product.json` | Branding + privacy overlay: names, app/data/bundle ids, fresh win32 GUIDs, URLs to the repo, extensionsGallery to Open VSX, enableTelemetry false, voiceWsUrl dropped, defaultChatAgent.extensionId off GitHub.copilot, GitHub auth trust emptied. Review fix: emptied the five defaultChatAgent Copilot **egress** URLs (entitlementUrl, tokenEntitlementUrl, mcpRegistryDataUrl, managedSettingsUrl, entitlementSignupLimitedUrl) that core `DefaultAccountProviderContribution` fetched at BlockStartup, and repointed the aka.ms/github-copilot plan/signup links to the brand domain. Theme follow-up: curated `onboardingThemes` (the welcomeOnboarding picker list) from the upstream Dark/Light 2026 + Solarized + HC set down to the four Clawdius themes. `defaultChatAgent.provider.default.name` `"GitHub"` → `"Clawdius"` — the central knob that flips ~20 interpolated `{0}` chat-setup strings and the default agent name to "Clawdius Copilot" (paired with the `chatListRenderer.ts` `COPILOT_USERNAME` edit above). Chat backend: added `extensionEnabledApiProposals` allowlisting `vscode.clawdius-chat` for the `defaultChatParticipant` + `chatParticipantAdditions` + `chatProvider` proposals (required once the product is BUILT/`isBuilt`; a dev run enables all proposals); `defaultChatAgent.extensionId`/`chatExtensionId` point at `vscode.clawdius-chat` (the built-in chat extension uses the `vscode` publisher, matching the other built-ins). Data-only. | n/a (data overlay) | product config |
| `src/vs/workbench/contrib/chat/common/languageModels.ts` | Chat backend: `COPILOT_VENDOR_ID` value `'copilot'` → `'clawdius'`. This constant is the "privileged default language-model vendor" used across the chat stack (default-model selection in `extHostLanguageModels.getDefaultLanguageModel`, the vendor descriptor `isDefault`, BYOK detection, model-picker display). Copilot is eliminated, so the `'copilot'` vendor never exists; repointing it at Clawdius's own vendor (the built-in clawdius-chat extension registers a Claude language-model provider with vendor `"clawdius"`) makes Claude the default model so the chat resolves a model instead of throwing "Language model unavailable". Constant NAME kept to minimize merge surface; only the value changes. | `// CLAWDIUS-BEGIN default vendor` | chat / language models |
| `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` | Chat backend: default the `chat.extensionUnification.enabled` setting to `false` (was `true`). Copilot's completions+chat "unification" is irrelevant in Clawdius (Copilot is deleted) and actively harmful: `ExtensionEnablementService._isDisabledByUnification` force-disables the extension whose id equals `product.defaultChatAgent.extensionId` — i.e. our built-in `vscode.clawdius-chat` (the Claude CLI backend) — dropping it from the extension registry so it NEVER activates (silently, no error). Defaulting OFF lets clawdius-chat be the live default chat extension. A product.json `configurationDefaults` override does NOT work here: the enablement service reads this setting before product.json defaults are applied, so the registered default must change. Also (chat parity): registers the new `ClaudeUsageStatusEntry` workbench contribution (import + `registerWorkbenchContribution2`) next to `ChatStatusBarEntry`. | `// CLAWDIUS-BEGIN no copilot unification`, `// CLAWDIUS` (usage entry import + registration) | chat / extension enablement |
| `src/vs/workbench/contrib/chat/browser/chatStatus/media/chatStatus.css` | Chat parity: appended `.clawdius-usage-*` styles for the Claude Code usage tooltip - the capacity bars (reusing `.quota-bar`/`.quota-bit`), the orange contribution heatmap, and the stats grid - styled to match Claude Code's `/usage` view. Also the status-bar content styles (the Claude sunburst mark + per-window capacity pips). | `/* CLAWDIUS-BEGIN claude usage entry */` | chat / status bar |
| `src/vs/workbench/contrib/editTelemetry/browser/editStats/aiStatsStatusBar.ts` | Chat parity: early-return the `AiStatsStatusBar` constructor when `defaultChatAgent.entitlementUrl` is empty (Clawdius), suppressing VS Code's own "AI Usage Statistics" / "Inline suggestions" status-bar entry so it does not duplicate the dedicated Claude Code usage entry. | `// CLAWDIUS-BEGIN no vscode AI-stats entry` | edit telemetry / status bar |
| `src/vs/workbench/services/chat/common/chatEntitlementService.ts` | Sign-in removal: the CHOKE POINT. `ChatEntitlementService` init bails when `defaultChatAgent.entitlementUrl` is empty (Clawdius), leaving `context`/`requests` undefined so `ChatSetupContribution` short-circuits and NONE of the Copilot sign-in/setup/growth chrome registers - the titlebar Sign In pill + toggle, the accounts-menu entry, the F1 setup commands, the upgrade/manage/hide items, the editor-context chat actions, the growth-session nudge, the setup agents, and the setup dialog. | `// CLAWDIUS-BEGIN no copilot setup chrome` | chat / entitlement |
| `src/vs/workbench/contrib/chat/browser/chatStatus/chatStatusEntry.ts` | Sign-in removal: inject `IProductService` and early-return the `ChatStatusBarEntry` constructor when `entitlementUrl` is empty, removing the status-bar "Sign In" entry (whose `CHAT_SETUP_ACTION_ID` command no longer exists). `runningSessionsCount` gets a `= 0` initializer since the early return can skip its constructor assignment. | `// CLAWDIUS-BEGIN no copilot status entry` | chat / status bar |
| `src/vs/sessions/browser/accountTitleBarState.ts` | Sign-in removal: `getCopilotPresentation` returns `undefined` when `entitlementUrl` is empty (static `product` import), so the sessions-window "Agents Signed Out" / "Copilot Unavailable" chip never renders. | `// CLAWDIUS-BEGIN no copilot signed-out chip` | sessions / account |
| `src/vs/workbench/contrib/welcomeOnboarding/browser/onboardingVariationA.ts` | Sign-in removal: drop the onboarding Sign-In step from the step sequence + suppress the "Sign in to use GitHub Copilot" footer nudge when `entitlementUrl` is empty. | `// CLAWDIUS-BEGIN no copilot sign-in onboarding` | welcome onboarding |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts` | Agents window: the session-source provider's user-visible display name `"Copilot Chat"` -> `"Claude Code"` and icon `Codicon.copilot` -> `Codicon.claude`. The provider id (`COPILOT_PROVIDER_ID`), the nls key, the class name, and all command ids are left intact so the Claude/CLI wiring + tests still bind. The `sessionTypes` getter omits `CopilotCloudSessionType` (the GitHub Copilot cloud coding agent) when `entitlementUrl` is empty, so a new session in the Agents window does not default to a dead Cloud type that shows "No models available" - the agent-host Claude agent is the sole/default new-session type. | `// CLAWDIUS-BEGIN claude branding (agents window session source)`, `// CLAWDIUS-BEGIN no copilot cloud session type` | sessions / agents window |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessions.contribution.ts` | Two Settings descriptions rebranded - "Copilot Chat sessions provider" -> "Claude Code sessions provider", and the Claude-agent enable setting drops the false "Uses your existing Copilot subscription" clause (Clawdius has no Copilot subscription). Setting keys unchanged. | `// CLAWDIUS-BEGIN claude branding (setting description)`, `// CLAWDIUS-BEGIN claude branding (drop false copilot-subscription claim)` | sessions / agents window |
| `src/vs/sessions/contrib/accountMenu/browser/account.contribution.ts` | account-avatar alt text `"GitHub profile image for {0}"` -> `"Profile image for {0}"` (screen-reader string). | `// CLAWDIUS-BEGIN no github branding` | sessions / account |
| `src/vs/sessions/contrib/providers/agentHost/browser/openSessionEventsFileActions.ts` | command-palette title `"Open Copilot CLI State File"` -> `"Open Agent Session State File"`. Command id unchanged. | `// CLAWDIUS-BEGIN no copilot branding` | sessions / agents window |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/permissionPicker.ts`, `.../mobilePermissionPicker.ts` | the shared permission-picker subtext (nls key `permissions.default.subtext`) `"Copilot uses your configured settings"` -> `"Claude uses your configured settings"`, changed identically in both the desktop and mobile pickers so the shared key stays single-valued. | `// CLAWDIUS-BEGIN claude branding` | sessions / agents window |
| `src/vs/workbench/contrib/chat/browser/agentSessions/agentSessions.ts` | the Agents-window Claude provider description dropped the misleading "the Claude models included in your GitHub Copilot subscription" and now reads "Delegate tasks to Claude. ...". Architecture-neutral pending the model-transport decision. | `// CLAWDIUS-BEGIN claude branding (no copilot subscription)` | chat / agents window |
| `src/vs/sessions/browser/sessionsSetUpService.ts` | Sign-in removal: `SessionsSetUpWidget._start()` early-returns `onCompleted()` when `entitlementUrl` is empty, suppressing the GitHub/Copilot-branded sessions welcome + sign-in dialog (there is no IDE account to sign into in Clawdius). Mirrors the `accountTitleBarState.ts` choke point. | `// CLAWDIUS-BEGIN no copilot setup/sign-in flow` | sessions / agents window |
| `src/vs/platform/agentHost/node/claude/claudeAgent.ts` | Agent-host Claude goes Copilot-free: in Clawdius mode (empty `entitlementUrl`) `getProtectedResources()` returns `[]` (no GitHub auth advertised), `_ensureAuthenticated()` returns `undefined` instead of throwing `AHP_AUTH_REQUIRED` (no proxy handle needed - the SDK subprocess authenticates via native `~/.claude` OAuth), and `_refreshModels()` publishes a static Claude catalog (`opus`/`sonnet`/`haiku` family aliases + effort schema) instead of querying the Copilot CAPI. The catalog is published from the constructor since `authenticate()` never runs without a Copilot resource. Upstream/Copilot path unchanged when `entitlementUrl` is present. | `// CLAWDIUS-BEGIN native ~/.claude auth`, `// CLAWDIUS-BEGIN static claude catalog` | agent host / claude |
| `src/vs/platform/agentHost/node/claude/claudeSdkOptions.ts` | `buildOptions(proxyHandle?)` is now optional; when undefined (Clawdius native mode) it omits the `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` CAPI-proxy overrides and `buildSubprocessEnv(false)` stops stripping `ANTHROPIC_API_KEY`, so the SDK subprocess inherits the env and resolves `~/.claude/.credentials.json` OAuth exactly like the `claude` CLI. Handle-present (Copilot) output is byte-identical. | `// CLAWDIUS-BEGIN native ~/.claude auth` | agent host / claude |
| `src/vs/platform/agentHost/node/claude/claudeAgentSession.ts` | widen `IMaterializeContext.proxyHandle` to optional so a Clawdius session materializes with no CAPI proxy. | `// CLAWDIUS-BEGIN native ~/.claude auth` | agent host / claude |
| `src/vs/platform/agentHost/node/agentHostMain.ts` | gate `CopilotAgent` (the `copilotcli` provider) registration on a non-empty `entitlementUrl` so it is NOT registered in Clawdius - the dead GitHub Copilot CLI agent only cluttered the Agents window picker and spammed "No token resolved" auth attempts. Leaves the agent-host Claude agent as the sole, default agent. | `// CLAWDIUS-BEGIN no copilot agent in the agents window` | agent host |
| `src/vs/platform/agentHost/node/agentHostServerMain.ts` | De-Copilot follow-up: the **remote/server** agent host registered `CopilotAgent` *unconditionally*, missing the `entitlementUrl` gate the desktop `agentHostMain.ts` already has - so a Clawdius remote/SSH/WSL/tunnel session would still spin up the GitHub Copilot agent. Now gated on `productService.defaultChatAgent?.entitlementUrl`, mirroring the desktop path. | `// CLAWDIUS-BEGIN no copilot agent in clawdius` | agent host |
| `src/vs/platform/agentHost/common/agentHost.config.contribution.ts` | De-Copilot follow-up: the `chat.editor.defaultProvider` setting's `enum`/`enumDescriptions` offered "Use the Extension Host Copilot CLI" / "Use the Agent Host Copilot CLI" (the suppressed `copilotEh`/`copilotAh` providers) in the Settings editor. Now conditional on `entitlementUrl`: Clawdius offers only `local`. (The two niche `chat.*.copilotCli.hideExtensionHost` toggles for the suppressed Copilot-CLI entry are left - inert, low-visibility; gating them out is a minor follow-up.) | `// CLAWDIUS-BEGIN no copilot cli provider options in the settings picker` | agent host / settings |
| `src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts` | `_isCopilotCliAvailable()` returns false in Clawdius so the residual "Copilot CLI" new-session type (a phantom entry with the Copilot label + icon; there is no EH Copilot CLI harness) is never offered. | `// CLAWDIUS-BEGIN no copilot cli session type` | sessions / ultracode |
| `src/vs/workbench/contrib/chat/browser/agentSessions/agentHost/agentHostSessionListController.ts`, `.../agentHostSessionHandler.ts` | native Claude session rows + the dynamic agent themeIcon + forked-session items rendered the Copilot logo; derive the icon from the session type (`...claude` -> `Codicon.claude`, else `Codicon.copilot`) so Clawdius shows the Claude logo. | `// CLAWDIUS-BEGIN claude icon on native session rows`, `... themeicon`, `... forked native session` | chat / agents window |
| `src/vs/sessions/contrib/providers/localChatSessions/browser/localChatSessions.contribution.ts` | suppress the Local (VS Code in-process) sessions provider in Clawdius. Its `getModels` returned every user-selectable model with no `targetChatSessionType`, surfacing the clawdius-chat extension vendor as a duplicate "Clawdius" model group alongside the native "Claude" group; suppressing it leaves the agent-host Claude as the sole group (and the Ultracode window native-Claude-only). | `// CLAWDIUS-BEGIN native-claude-only sessions (dedupe model picker)` | sessions / ultracode |
| `src/vs/platform/agentHost/node/claude/claudeAgent.ts` (catalog) | bump opus + sonnet `maxContextWindow` to 1_000_000 (the opus 4.6+/sonnet 4.6 aliases are 1M-context by default on the native SDK path); haiku stays 200K. | `// CLAWDIUS-BEGIN static claude catalog` | agent host / claude |
| `src/vs/workbench/contrib/chat/browser/widget/input/chatModelPicker.ts` | show the clickable effort + context-size pills (the "Ultracode effort" selector) in Clawdius. Upstream gates them on usage-based-billing; Clawdius (empty `entitlementUrl`) is always PRU, so `showConfigPills` also enables them there (click handlers are wired unconditionally; the value otherwise only appeared as a label suffix). Upstream PRU unchanged. | `// CLAWDIUS-BEGIN show effort/tokens pills in clawdius (PRU) mode` | chat / model picker |
| `extensions/clawdius-chat/src/extension.ts` (panel models) | the panel chat model picker showed only the synthetic "Auto" because the LM provider never fired a change event, so the core never eagerly resolved its static models. Added `onDidChangeLanguageModelChatInformation` + fire it once after registration -> core resolves opus/sonnet/haiku into the renderer cache. | n/a (extension; ships only in Clawdius) | clawdius-chat extension |
| `src/vs/sessions/contrib/workflows/browser/workflows.contribution.ts`, `.../browser/workflowsView.ts`, `.../common/workflows.ts`, `.../browser/media/workflows.css` (new) + `src/vs/sessions/sessions.common.main.ts` (import) | A new "Workflows" view container in the Ultracode auxiliary (secondary side) bar - modeled on the `changes` contribution - with a `WorkbenchObjectTree` that renders Claude-native multi-agent workflow runs (root: name + status + agent count + duration + tokens + model) expandable to their sub-agents (label + type + tokens + tools), sourced from `WorkflowStore`. Running workflows sort to the top with a spinning status icon and update live (verified: a CLI-spawned run appeared in the board with a live spinner for its full ~50s lifetime, then flipped to completed). Opening a sub-agent row (`onDidOpen`) drills into its transcript (see the `workflowTranscript.ts` row). Whole-workflow controls land next. | `// CLAWDIUS-BEGIN ultracode workflows view` | sessions / ultracode |
| `src/vs/sessions/contrib/workflows/common/workflowStore.ts` (new) | `WorkflowStore` reads the durable Claude Code "Ultracode" workflow-run records the CLI writes to `~/.claude/projects/<encoded-cwd>/<sessionId>/workflows/wf_*.json` (one self-describing multi-agent run per file) and projects them to a typed `IWorkflowRun[]` (name/status/agents/tokens/model/phases, newest first) for the Workflows board. Scans all projects (global) because a run is stored under the CLI session's cwd, often an ancestor of the window's workspace. Also surfaces *in-progress* runs by reading the incremental `subagents/workflows/<runId>/journal.jsonl` (started/result events) + per-agent `agent-*.meta.json` the CLI writes during a run (the self-describing `wf_*.json` summary is only written at completion); a 2.5s live poll (with a short idle grace period to survive the completion handoff) keeps running rows fresh, and running runs dedupe against completed ones on `runId`. Validated against 90 real runs on disk plus a live CLI-spawned run. | `// CLAWDIUS-BEGIN ultracode workflow store` | sessions / ultracode |
| `src/vs/sessions/contrib/workflows/browser/workflowTranscript.ts` (new) + registration in `workflows.contribution.ts` | Drill-in from a workflow sub-agent row to its transcript. Each `IWorkflowAgent` now carries the on-disk `agent-<id>.jsonl` path (`<sessionDir>/subagents/workflows/<run>/`, derived from the summary filename stem for completed runs and the live run dir for in-progress ones). A text-model content provider on the `clawdius-workflow-transcript:` scheme renders that JSONL as read-only Markdown - User/Assistant turns, tool calls with their JSON input, and tool results (each block capped, fences sized to the content) - so the user sees what a sub-agent did, the same as in the CLI. The source `.jsonl` path rides in the virtual resource's query, so no temp files are written. Verified live: clicking a completed run's sub-agent opened its formatted transcript in the Ultracode editor area. | `// CLAWDIUS-BEGIN ultracode workflow transcript` | sessions / ultracode |
| `src/vs/sessions/contrib/workflows/common/workflowStore.ts` + `.../browser/workflowsView.ts` + `.../browser/media/workflows.css` | Cancel a running workflow this window spawned, and honestly mark the rest view-only. The store cross-references each running run's `sessionId` against the agent host (`IAgentHostService.listSessions()` + a held `StateComponents.Session` subscription per candidate) and treats a run as controllable only when the window is *actively driving its turn* (`SessionState.activeTurn` is present) - because `listSessions` returns *persisted* sessions and the terminal CLI persists into the same `~/.claude` project, on-disk id-match alone is too loose. `controlFor(run)` drives a row badge ("in window" vs "external"); a right-click menu offers `cancelWorkflow(run)` (dispatches `SessionTurnCancelled` — the same path the chat stop button uses → `abortSession` + sub-agent cancellation) and `steerWorkflow(run, msg)` (dispatches `SessionPendingMessageSet` with `PendingMessageKind.Steering` — the chat input's mid-turn steer, injected with 'now' priority). Per-sub-agent/interrupt-step controls are out of scope (the SDK only wires whole-turn abort; `Query.interrupt`/`stopTask` aren't plumbed). Verified: a running terminal-CLI workflow shows "external" and offers no controls; the classification + cancel + steer dispatch logic is covered by `test/common/workflowStore.test.ts` (14 tests), and the dispatches reuse the in-product chat stop/steer paths. | `// CLAWDIUS-BEGIN ultracode workflow store`, `// CLAWDIUS-BEGIN ultracode workflows view` | sessions / ultracode |
| `src/vs/workbench/contrib/chat/common/plugins/agentPluginServiceImpl.ts` (new `ClaudeCliAgentPluginDiscovery`) + `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` (registration) | Discover plugins installed by the local Claude Code CLI under `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/` so a user's CLI plugins appear in the Ultracode Customizations panels - with their bundled skills/agents/commands/hooks + `.mcp.json` MCP server, which `detectPluginFormat` (already returns CLAUDE_FORMAT for `.claude` paths), the base `_toPlugin`, and `PluginMcpDiscovery` wire up automatically. Resolves the active version from `installed_plugins.json` `installPath` (not a glob, since multiple versions coexist) and respects `settings.json` `enabledPlugins`. READ-ONLY mirror (no `remove`). Registered in place of `CopilotCliAgentPluginDiscovery` when `entitlementUrl` is empty. | `// CLAWDIUS-BEGIN claude cli plugin discovery` | chat / plugins |
| `src/vs/sessions/electron-browser/parts/titlebarPart.ts`, `src/vs/sessions/common/categories.ts`, `src/vs/workbench/contrib/chat/electron-browser/agentSessions/agentSessionsActions.ts` | Ultracode rename: the user-visible "Agents" window name -> "Ultracode" at the window title, the command-palette category, and the Open/Open-in Ultracode (Window) action titles + titlebar hover. Internal ids/keys/context-keys/CSS/the `isSessionsWindow` concept + the Customizations "Agents" (=Claude sub-agents) section are unchanged. | `// CLAWDIUS-BEGIN ultracode window name` | sessions / ultracode |
| `src/vs/platform/agentHost/common/agentHost.config.contribution.ts` | default `chat.agentHost.enabled` ON for Clawdius (so the agent host spawns in dev AND stable builds) - flipped in-place because this key is read in the main process where product.json `configurationDefaults` are not applied. | `// CLAWDIUS-BEGIN agent host on for clawdius` | agent host / config |
| `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` | default `chat.agents.claude.preferAgentHost` and `chat.editor.claude.preferAgentHost` to `true` in Clawdius so the agent-host Claude provider surfaces (and the EH provider stands down). Upstream keeps `default: false`. | `// CLAWDIUS-BEGIN prefer agent-host claude` | chat / config |
| `src/vs/platform/agentHost/test/node/claudeAgent.test.ts`, `.../claudeAgent.integrationTest.ts` | give the test product service a non-empty `entitlementUrl` so the existing tests keep exercising the upstream Copilot/CAPI path (it gates the new Clawdius branch); add a `productService` override hook to `createTestContext` and a dedicated "Clawdius native mode" suite asserting `getProtectedResources()` is empty, the static catalog publishes at construction, and `createSession` runs with no auth gate. Also registers `IProductService` in the integration-test service collections (it was missing). | `// CLAWDIUS-BEGIN copilot-path product service`, `// CLAWDIUS-BEGIN clawdius native-mode coverage` | agent host / tests |
| `src/vs/workbench/services/themes/common/workbenchThemeService.ts` | Default color theme constants set to Clawdius Dark/Light; and the pre-theme first-paint `COLOR_THEME_*_INITIAL_COLORS` accent hexes recolored from Microsoft blue (#0078D4/#005FB8) to the Clawdius orange family, so first paint is Clawdius not blue (neutrals + semantic add/delete/error colors untouched; chat-surface accents left for a later pass). Welcome-picker follow-up: `COLOR_THEME_HC_DARK`/`COLOR_THEME_HC_LIGHT` repointed from upstream `Default High Contrast`/`Default High Contrast Light` to the new `Clawdius High Contrast`/`Clawdius High Contrast Light` themes (folded into the same marked region). | `// CLAWDIUS-BEGIN default theme`, `// CLAWDIUS-BEGIN initial-colors accent recolor` | theme service |
| `build/hygiene.ts` | Inverted the upstream "product.json must not contain extensionsGallery" guard into "Open VSX only" (exact `https://open-vsx.org/` host prefix across serviceUrl/itemUrl/resourceUrlTemplate) so the baked-in Open VSX gallery passes hygiene while the Microsoft Marketplace stays blocked. `checkCopilotEnginesVersion` short-circuits to `undefined` when `extensions/copilot/package.json` is absent (it was deleted), instead of crashing the precommit hook with ENOENT on the missing manifest. | `// CLAWDIUS-BEGIN gallery policy`, `// CLAWDIUS-BEGIN copilot eliminated` | build hygiene |
| `build/npm/dirs.ts` | Fallout: dropped `'extensions/copilot'` from the npm install-target list. `postinstall.ts` runs `npm install` in each listed dir with `cwd` set to it; after the extension was deleted, the missing `cwd` made Node throw `spawn /bin/sh ENOENT`, failing `npm ci` (and thus every CI run) at the postinstall step. This was the lone straggler from the wholesale deletion - `build/hygiene.ts` and `build/lib/extensions.ts` were already guarded. | `// CLAWDIUS-BEGIN no copilot built-in extension` | build / npm install |
| `src/vs/workbench/services/accounts/browser/defaultAccount.ts` | Review fix: `DefaultAccountProviderContribution` (registered at `WorkbenchPhase.BlockStartup`) skips registering the GitHub/Copilot default-account provider when `defaultChatAgent.entitlementUrl` is empty (Clawdius empties it), closing ALL startup account egress at the source — incl. the **enterprise** branch that reconstructs `api.{ghe-host}/copilot_internal/*` from user settings, which emptying the product URLs alone did not catch. `DefaultAccountService` opens its init barrier in that no-provider case so `getDefaultAccount()/refresh()/signIn()/signOut()` resolve to "no account" instead of hanging forever (provider registration is otherwise the only `initBarrier.open()`). | `// CLAWDIUS-BEGIN no default-account egress`, `// CLAWDIUS-BEGIN no default-account` | account service |
| `src/vs/workbench/contrib/welcomeGettingStarted/common/media/theme_picker.ts`, `theme_picker_small.ts` | Welcome "Pick a Color Theme" walkthrough: rebranded the four tile labels (Dark Modern / Light Modern / Dark High Contrast / Light High Contrast) to Clawdius Dark / Clawdius Light / Clawdius High Contrast / Clawdius High Contrast Light. The tiles already *applied* the correct themes via the `ThemeSettingDefaults.COLOR_THEME_*` constants; this fixes the visible labels. Markers wrap the module outside the HTML template literal (JS comments cannot live inside it). | `// CLAWDIUS-BEGIN theme-picker labels` | welcome / getting-started |
| `src/vs/workbench/contrib/welcomeGettingStarted/common/media/dark.png`, `light.png`, `dark-hc.png`, `light-hc.png` | Regenerated the four welcome-picker preview thumbnails (874×600) as recolored mockups built from each Clawdius theme's real editor/sidebar/token/accent colors (orange tab indicator + accent pill; the HC variants keep the white-on-black / black-on-white high-contrast frame and divider). Replaces the upstream Modern/HC preview art. | n/a (binary asset) | welcome / getting-started |
| `extensions/theme-defaults/package.json` | "Only Clawdius themes": removed the 10 upstream color themes (Dark/Light Modern, Dark/Light+, VS Dark/Light, Dark/Light 2026, Default High Contrast + Light) from `contributes.themes`; kept the `vs-minimal` icon theme. The inert `themes/*.json` files are left untouched (so they merge cleanly) but are no longer contributed. | n/a (JSON, no inline comments) | built-in themes |
| `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts` (7 settings strings), `.../chat/browser/actions/chatActions.ts`, `.../chat/browser/actions/openCopilotCliStateFileAction.ts` (3), `.../chat/browser/widget/input/permissionPickerActionItem.ts`, `.../welcomeGettingStarted/common/gettingStartedContent.ts` (3) | Brand-string sweep (#32): rebranded 15 residual user-visible Copilot/GitHub `localize` strings that a Clawdius user actually sees and that were NOT already gated or rebranded. Surfaces: Settings editor descriptions (`chat.disableAIFeatures` "GitHub Copilot"→"Claude"; `chat.titleBar.signIn.enabled` drop "Copilot"; `chat.useClaudeHooks` + `chat.hookFilesLocations` "Copilot[-format] hooks"→"built-in hook format"; `chat.growthNotification` "try Copilot"→"try Claude"; `chat.agentHost.customTerminalTool.enabled` + `chat.agentHost.sdkSandbox.enabled` "Copilot SDK"→"agent SDK"); command-palette titles ("Show Extensions using Copilot"→"Claude"; "Open Copilot CLI State File"→"Open Agent Session State File"); two `openSessionEventsFile` notification toasts (→ neutral "agent session" wording, matching the already-rebranded sessions-app sibling); the chat-input approvals picker subtext ("Copilot uses your configured settings"→"Claude…", matching the sibling permission pickers); and the "Get started" welcome walkthrough step title/description/alt-text ("Copilot for free"→"Claude", "[Copilot]({0})"→"[Claude]({0})"). All localize KEYS unchanged; each edit wrapped in markers. Found+verified by an 8-agent discover→adversarial-verify→synthesize workflow (receipts in private notes). | `// CLAWDIUS-BEGIN claude branding`, `// CLAWDIUS-BEGIN no copilot branding` | chat / welcome / settings |
| `src/vs/workbench/contrib/chat/common/languageModelStats.ts`, `src/vs/platform/agentHost/common/agentHostSchema.ts` | Follow-on (#32): two more user-visible Copilot leaks. `languageModelStats.ts` — the extension Features-tab feature label + icon ("Copilot" + `Codicon.copilot` → "Claude" + `Codicon.claude`), pairing with the "Show Extensions using Claude" command rebrand. `agentHostSchema.ts` — the auto-approve `defaultDescription` enum ("Copilot uses your configured settings" → "Claude…"), the same string already rebranded in the three sibling permission pickers. | `// CLAWDIUS-BEGIN claude branding` | chat / agent host |
| `src/vs/workbench/contrib/chat/browser/actions/chatActions.ts`, `.../actions/chatNewActions.ts`, `.../actions/createPluginAction.ts` | Brand-icon sweep (#44): rebranded 3 user-facing `Codicon.copilot` (GitHub Copilot logo) icons to `Codicon.claude` — the "New Chat Editor" editor-title button + the New Chat button's `copilot` icon variant (both shown when the `chatNewButtonIcon` A/B experiment selects 'copilot', which the Clawdius gate doesn't suppress), and the "Agents" group header icon in the Create Plugin quick pick (the lone brand logo among semantic codicons; plugins are a Clawdius feature). A 10-candidate reachability workflow left the other 7 (verified): the generic profile-icon palette, the gated quota dialog + setup runner, and the suppressed Copilot-CLI / AgentHostCopilot provider→icon paths that never render in Clawdius. For `chatNewActions.ts` only the `icon:` changed — `idSuffix`/`iconValue` are experiment keys. | `// CLAWDIUS-BEGIN claude branding` | chat / icons |
| `extensions/theme-{abyss,kimbie-dark,monokai,monokai-dimmed,quietlight,red,solarized-dark,solarized-light,tomorrow-night-blue}/**` | "Only Clawdius themes": deleted the 9 standalone upstream color-theme extensions wholesale (each contributed exactly one color theme and nothing else), so only the four Clawdius themes ship. `theme-seti` (default file icons) and `theme-defaults` (icon theme) are kept. `branding-guard.ts` scans every extension manifest and fails if any non-Clawdius color theme reappears (e.g. via an upstream merge). | n/a (upstream dir removal) | built-in themes |
| `build/gulpfile.extensions.ts` | Registered the new in-tree `extensions/clawdius-chat` extension in the hardcoded `compilations` list so its TypeScript compiles (the auto-glob is commented out upstream, so a new extension is not picked up automatically). | `// CLAWDIUS-BEGIN clawdius-chat` | build / extensions |
| `src/vs/workbench/contrib/chat/browser/widget/chatListRenderer.ts` | `COPILOT_USERNAME` `'GitHub Copilot'` → `'Clawdius Copilot'`, in lockstep with `product.json` `provider.default.name` → `'Clawdius'` (the default agent's display name is `${provider.default.name} Copilot`), so the redundant chat username + avatar stay hidden in the transcript. Must move together with the product.json flip. | `// CLAWDIUS-BEGIN brand username` | chat |
| `extensions/copilot/**` | "Copilot eliminated". Deleted the GitHub Copilot Chat extension wholesale (the prebuilt `dist/` bundle + 4154 tracked files). It registered SIX competing `isDefault` panel chat participants (`github.copilot.default/editingSession/editsAgent/...`) + 9 `chatViewsWelcome` sign-in overlays on `onStartupFinished`; since the panel default is "last isDefault extension agent wins" (`_preferExtensionAgent`), it raced the Clawdius participant for the panel nondeterministically. With it gone, `clawdius-chat` deterministically owns the panel. Build-safe: core references `github.copilot.*` only as product.json strings + context-key names (no TS import — core type-check stays green), and `packageCopilotExtensionStream` (build/lib/extensions.ts) already guards a missing dir with `fs.existsSync` → returns an empty stream, so the dead `compile-copilot-extension-build` gulp task (still wired into the packaging series in gulpfile.vscode.ts/reh.ts) is a harmless no-op. Those upstream build files are deliberately LEFT UNEDITED to keep the merge surface minimal. `branding-guard.ts` now fails if `extensions/copilot` reappears. | n/a (upstream dir removal) | built-in extensions (Copilot) |
| `eslint.config.js` | Fallout: the root flat-config imported a custom ESLint plugin from INSIDE the deleted extension (`./extensions/copilot/.eslintplugin/index.ts`) and carried ~400 lines of copilot-only lint blocks (7 config objects, lines 2493-2892, using the `copilot-local` plugin). Removed the import + all seven blocks + two now-dead `extensions/copilot/**/*` ignore globs, so the config loads without the missing module (27 blocks remain). Left untouched at the time: the `@github/copilot-sdk` / `@vscode/copilot-api` restricted-import entries and the `copilotChatSessions` sessions-provider zone (UPDATE: the two restricted-import entries were SINCE removed in the deeper agent-host elimination; only the `copilotChatSessions` provider zone, 2 refs, remains) — those belong to the agentHost subsystem + Agents window, a separate deeper elimination, not this extension. | n/a (block + import removal) | lint config |
| `package.json`, `.vscode/tasks.json` | Autonomous push (de-Copilot build residue): the deleted `extensions/copilot` left dead build members that broke `npm run watch`/`compile` locally (CI was unaffected - it type-checks via `compile-check-ts-native`, not the gulp build). Removed the standalone `compile-copilot`/`watch-copilot`/`watch-copilotd`/`kill-watch-copilotd`/`copilot:setup`/`copilot:get_token` scripts and pruned the copilot members from the `compile`/`transpile`/`watch`/`watch-transpile` aggregates in package.json; removed the `Copilot - Build`/`Kill Copilot - Build` tasks plus their `dependsOn` entries in tasks.json. clawdius-chat is a standard extension built by the core `watch-extensions`/`compile-client` pipeline (gulpfile.extensions.ts:55), so no replacement watcher is needed. The upstream `compileCopilotExtensionBuildTask` no-op in gulpfile.extensions.ts is LEFT UNEDITED (existsSync-guarded). | n/a (dead-script removal) | build scripts / tasks |
| `src/vs/platform/agentHost/node/claude/claudeSdkOptions.ts`, `.../claudeAgentSession.ts`, `.../agentHostMain.ts`, `.../agentHostServerMain.ts`, `src/vs/platform/agentHost/common/agentHost.config.contribution.ts` | Native redesign, CLI backend resolution: thread the new `IClawdiusCliConfigService` (`src/vs/platform/clawdius/**`, new dir) into the agent-host Claude path so the engine (bundled SDK cli.js vs the user-installed cli.js) + provider env resolve from `clawdius.cli.*` settings. `claudeSdkOptions.ts`: `IBuildOptionsInput` gains required `cliResolution`; `buildOptions` maps `executable` ('node' -> `process.execPath`, Electron-as-node, preserving bundled behavior), adds `pathToClaudeCodeExecutable` in userCli mode, overlays `extraEnv` with the subprocess scrub winning (so user env cannot reintroduce stripped NODE_OPTIONS/VSCODE_*/ELECTRON_*). `claudeAgentSession.ts`: injects the service, resolves at materialize + rematerialize. `agentHostMain.ts`/`agentHostServerMain.ts`: register the service after `registerAgentHostNetworkServices`. `agentHost.config.contribution.ts`: registers the `clawdius.cli.*` settings, gated to Clawdius mode. Resolution is file-existence-only (no spawn/network; branding-guard now asserts it). | `// CLAWDIUS-BEGIN cli backend resolution` / `// CLAWDIUS-BEGIN clawdius cli engine settings` | agent host / claude |
| `src/vs/workbench/workbench.common.main.ts`, `src/vs/sessions/sessions.common.main.ts` | Native redesign, main-IDE Clawdius container: `workbench.common.main.ts` adds a side-effect import of the new gated `contrib/clawdius/browser/clawdius.contribution.js` (a native Sidebar view container + Workflows view + transcript provider, all in the MAIN workbench, gated on empty `entitlementUrl`). `sessions.common.main.ts` DROPS its side-effect import of `contrib/workflows/browser/workflows.contribution.js` so the legacy sessions-window Workflows pane no longer registers (the feature now lives in the main IDE). The old `src/vs/sessions/contrib/workflows/**` tree has since been deleted from disk; extracting its agent-host control half into a control provider remains a tracked follow-up. | `// CLAWDIUS-BEGIN native main-IDE clawdius container` / `// CLAWDIUS-BEGIN ultracode workflows view moved` | workbench / sessions bootstrap |
| `src/vs/workbench/contrib/chat/common/constants.ts`, `.../chatSessionsService.ts`, `.../widgetHosts/editor/chatEditorInput.ts`, `src/vs/platform/agentHost/common/agentHost.config.contribution.ts`, `src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarActions.ts` | Native redesign, native chat on the RIGHT: make the existing right-hand chat panel default to the NATIVE agent-host Claude session in Clawdius mode (replacing the clawdius-chat `claude -p` shim as the default new-session backend), and brand the secondary-side-bar toggle. `chatSessionsService.ts`: add `SessionType.AgentHostClaude=agent-host-claude`. `constants.ts`: `getDefaultNewChatSessionType` maps the `claudeAh` provider value to `SessionType.AgentHostClaude` (no sync contribution gate; async-activates, panel falls back to local). `agentHost.config.contribution.ts`: `chat.editor.defaultProvider` offers + defaults to `claudeAh` in Clawdius mode, and the `experiment:{mode:startup}` override is gated to upstream so the Clawdius default is static. `chatEditorInput.ts`: fall back to a fresh local session when an UNTITLED contributed session (the agent-host-claude default) fails to resolve, so a new chat editor never opens blank. `auxiliaryBarActions.ts`: the secondary-side-bar toggle icon becomes `Codicon.claude` in Clawdius (a chat affordance, Kiro-style) instead of the right-pane layout glyph. The agent-host Claude session is served by the existing AgentHostSessionHandler; the workbench ChatWidget renders it natively (tools/permissions/diffs). | `// CLAWDIUS-BEGIN native agent-host Claude ...` / `// CLAWDIUS-BEGIN Claude chat-bubble toggle icon` | chat / agent host / workbench layout |
| `src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarActions.ts`, `src/vs/workbench/browser/actions/layoutActions.ts` | Native redesign, toggle LABELS: in Clawdius the secondary side bar IS the native Claude chat, so its show/hide toggle reads "Claude Code Chat" everywhere it surfaces - the top-bar `LayoutControlMenu` hover tooltip, the "Customize Layout" quick-pick entry (with the Claude mark instead of the layout glyph), the command-palette command, the Appearance-menu mnemonic, the pane-title hide tooltip, and the screen-reader visibility alerts - instead of "Secondary Side Bar". All gated on empty `entitlementUrl`; upstream keeps the layout wording + contextual icon. `layoutActions.ts` gains a `product` import + `clawdiusChatToggle` gate; the upstream-equivalent label branch uses a fresh `hideSecondarySideBar` NLS key to avoid a duplicate-key hygiene clash with the existing `closeSecondarySideBar` localize2 call. | `// CLAWDIUS-BEGIN Claude chat-bubble toggle label` / `// CLAWDIUS-BEGIN secondary side bar is the native Claude chat` | workbench layout |
| `src/vs/workbench/workbench.common.main.ts` (in-place import) + NEW dir `src/vs/workbench/contrib/clawdius/browser/chat/**` + `src/vs/workbench/contrib/clawdius/common/clawdius.ts` (ids) | Native WEBVIEW Claude chat (shell): the right-pane chat is rebuilt as a faithful native replica of the official Claude Code plugin's webview chat (NOT the workbench `ChatWidget`, which read as Copilot). `clawdiusChatViewPane.ts` is a first-party `ViewPane` hosting an `IWebviewService.createWebviewElement` webview whose inline SPA (warm palette, header, message list, welcome state, composer) is the bridge surface; the ViewPane relays composer input over `postMessage` and will later own an `IAgentHostService` Claude session. `clawdiusChat.contribution.ts` registers it as a NON-default (`isDefault:false`) AuxiliaryBar container gated on empty `entitlementUrl`, so the existing chat stays default until the live round-trip lands. CSP `default-src 'none'` (zero egress); WCAG-AA send button (`#b85c40`); validated via a 5-dimension adversarial workflow (D4 hygiene/layering clean). The workbench.common.main.ts change is one marked side-effect import. | `// CLAWDIUS-BEGIN native webview Claude chat` | chat / workbench layout |
| `src/vs/workbench/contrib/clawdius/browser/chat/clawdiusChatViewPane.ts` (same NEW file) | Live agent-host text round-trip: the ViewPane now drives a real Claude turn. On the first submit it lazily creates a `claude` session via `IAgentHostService.createSession({provider:'claude', workingDirectory})`, dispatches a `SessionTurnStarted` action, subscribes to `StateComponents.Session`, and streams the assistant's text into the SPA by re-deriving the active turn's `ResponsePartKind.Markdown` parts from the authoritative `SessionState` on each `onDidChange` (no fragile delta accumulation). Pending/streaming/done/error states, turn serialization (ViewPane `_isTurning` + SPA `busy`), sticky auto-scroll, and full teardown (cancel turn -> `disposeSession` -> dispose session-scoped store, guarded by a `_disposed` flag). Tools/thinking/diffs/permissions come in later increments. Protocol usage verified clean against the gold-standard `agentHostSessionHandler.ts` by a 5-dimension adversarial workflow; lifecycle/edge-case findings folded in. Imports `IAgentHostService` + agent-host state types from `platform/agentHost/common` (legal layering). | `// CLAWDIUS-BEGIN native webview Claude chat` | chat / agent host |
| `src/vs/workbench/contrib/clawdius/browser/chat/**`, `src/vs/workbench/workbench.common.main.ts` (import) | **Plugin-adoption pivot (supersedes rows above for the chat surface):** the bespoke native webview chat (`contrib/clawdius/browser/chat/**`) was RETIRED in favour of installing + driving the official `anthropic.claude-code` plugin's own pane (see `clawdiusPluginSetup.ts`). The `clawdius-chat` extension + `defaultChatAgent` product contract are KEPT as the hidden Clawdius-mode sentinel; the plugin owns the visible chat. | `// CLAWDIUS-BEGIN` (removed) | chat (retired) |
| `src/vs/workbench/contrib/clawdius/common/clawdiusConfig.ts`, `browser/clawdiusConfigStore.ts`, `browser/clawdiusConfigViewPane.ts`, `browser/clawdiusConfigActions.ts` (new) + `browser/media/clawdiusConfig.css`, `browser/media/clawdius-logo.svg` (new) + `clawdius.contribution.ts` / `common/clawdius.ts` (edits); DELETED `browser/workflowsViewPane.ts`, `browser/workflowTranscript.ts`, `common/workflowStore.ts`, `browser/media/workflows.css` + 2 tests | Replaced the Ultracode "Workflows" pane in the main-IDE Clawdius container with a **"Claude Code Config" manager** modeled on drewipson/claude-code-config + Kiro. EIGHT collapsible section views (Memories, Slash Commands, Skills, Sub-Agents, Hooks, Permissions, MCP, Plugins), each grouping **Global** (`~/.claude`) + per-folder **Project** (`.claude`) items, fed by one shared `IClawdiusConfigService` singleton (one scan + correlated watchers, coalesced refresh). Reads LOCAL files only (frontmatter / markdown-heading / JSONC parse; no network). Full CRUD: per-section title `+` (`MenuId.ViewTitle`) + view-welcome button create templated files (memories / commands / skills / sub-agents) or JSONC entries via `IJSONEditingService` (hooks / permissions / MCP, preserving formatting); right-click **Delete** trashes files / folders (`useTrash`) or removes the JSONC entry. Items carry `backing` (File/Folder/Jsonc) + `jsonPath` / `canDelete` / `canMove` metadata (move-scope is a follow-up). Activity-bar icon = the Clawdius logo (distinct from the plugin's Claude mark). | `// CLAWDIUS-BEGIN Claude Code Config ...` | clawdius / config |
| `browser/control/**` (Control Center editor pane) + DELETED `browser/clawdiusConfigViewPane.ts`, `browser/media/clawdiusConfig.css`, `browser/media/clawdius-logo.svg`, `common/clawdius.ts`; edits to `clawdius.contribution.ts` / `browser/clawdiusConfigActions.ts` / `common/clawdiusConfig.ts` | **Config-tree retirement (supersedes the row above):** the 8-section "Claude Code Config" sidebar tree was retired for a tabbed **Control Center** EditorPane (Usage / Permissions / MCP / Skills / Plugins / Hooks) with inline editing. `IClawdiusConfigService` (`clawdiusConfigStore.ts`) is KEPT - the Control Center reuses `configService.snapshot` and the shared create/delete commands; the old view container, its `clawdius-logo` activity-bar icon, and the view-only actions/IDs are removed. | `// CLAWDIUS-BEGIN Control Center` | clawdius / config |
| `src/vs/workbench/contrib/clawdius/browser/usage/**` (new) + `browser/parts/statusbar/statusbarModel.ts`, `browser/parts/globalCompositeBar.ts` (edits) | **Claude Code usage** indicator + dashboard, all from the user's own local `~/.claude` files (`stats-cache.json` + a capacity cache; the ONLY egress is a user-initiated `/api/oauth/usage` refresh when the dashboard is opened, never on restore). Status-bar entry: Claude logo (`currentColor`) + an inline CLI-style block-shade session bar; hover popup with Session / Weekly CSS bars + an account header + "Open Usage Dashboard"; a native DOM `EditorPane` dashboard (CSS / SVG limit bars, contribution heatmap, per-model line chart, hour bars; `clawd-dance.svg` hero). Opened from the indicator OR the bottom-left Account button. Suppresses the official plugin's own status widget via `StatusbarViewModel`. No "cost" shown (usage + limits only); limits hidden for Bedrock / Vertex / custom providers. | `// CLAWDIUS-BEGIN ... usage ...` | clawdius / usage |
| `src/vs/workbench/browser/parts/editor/media/letterpress-{dark,light,hcDark,hcLight}.svg`, `editorgroupview.css` | Empty-editor watermark = the Clawdius welcome-art mark: **animated** `welcome-art-{dark,light}-animated.svg` for the regular dark / light themes, the static welcome-art for the two high-contrast variants; `.letterpress` `aspect-ratio: 440/189; max-width: 340px`. | n/a (asset swap) | workbench / editor |
| `product.json` (`trustedExtensionPublishers`), `src/vs/workbench/contrib/clawdius/browser/clawdiusPluginSetup.ts` | First-run setup installs the two **jeanp413** remotes from Open VSX alongside `anthropic.claude-code` - `jeanp413.open-remote-ssh` (desktop / `isNative`) + `jeanp413.open-remote-wsl` (Windows desktop / `isNative && isWindows`) - via an `EXTENSIONS` list with `when` / `critical` flags (optional installs are best-effort; only the Claude Code engine is critical and retries next launch), `DONE_KEY` bumped to `.v2`. `product.json` adds `jeanp413` to `trustedExtensionPublishers` (with `anthropic`) so their unsigned-on-Open-VSX packages are publisher-trusted. | `// CLAWDIUS-BEGIN install + configure ...` | extensions / first-run |
| `src/vs/workbench/contrib/welcomeOnboarding/browser/onboardingVariationA.ts` + `media/variationA.css` + `media/theme-preview-clawdius-*.svg` (new) + `common/onboardingTypes.ts` | Onboarding (Variation A) rebrand: hero/agent-sessions retitled ("Build with Claude Code"), the personalize-step tip says "Clawdius commands", the four theme cards now load `theme-preview-clawdius-{dark,light,hc-dark,hc-light}.svg` (created from the upstream previews with the active-tab accent recolored to Clawdius orange - the previews were blank because the Clawdius theme ids had no matching SVG), the sign-in step's GitHub/Google/Apple buttons + Copilot disclaimer are gated off and the footer button is the primary "Continue" (the Sign-In step is already filtered out of `steps` in Clawdius), and the modal scrim drops `backdrop-filter: blur` for a solid dim (the blur bled across the title-bar seam and fuzzed the window controls). | `// CLAWDIUS-BEGIN claude branding` | welcome / onboarding |
| `welcomeGettingStarted/browser/gettingStarted.ts` (subtitle), `.../gettingStartedService.ts` (walkthrough gate), `helpActions.ts` (Ask @vscode + newsletter) | Welcome page: subtitle "Editing evolved" -> "Build with Claude Code"; the "Get started with VS Code" Setup walkthrough is suppressed in Clawdius (registration skips `category.id === 'Setup'`). Help menu: "Ask @vscode" command + menu item gated out of Clawdius (it opens a Copilot `@vscode` chat agent we do not ship); newsletter action retitled. | `// CLAWDIUS-BEGIN claude branding` / `// CLAWDIUS-BEGIN no "Ask @vscode" ...` | welcome / help |
| ~20 source files across issue-reporter, notifications/status, chat/MCP, getting-started, onboarding | Display-text brand sweep for the alpha (read-only scour then applied): user-visible "VS Code" / "Visual Studio Code" -> "Clawdius" in the Issue Reporter (source labels, version rows, diagnostics), reload/relaunch/locale/console/theme notifications, the SSH "Installing ... CLI on remote" progress, and chat/MCP descriptions. Only `localize()` message text changed - command ids, setting keys, `aka.ms/vscode-*` URLs, and the `vscode.*` API namespace are untouched. KNOWN-DEFERRED (your call, release-time): Linux/Windows packaging metadata (appdata/snapcraft/AppxManifest), built-in extension `package.nls.json` descriptions, and the `Microsoft` publisher metadata on the js-debug builtins (accurate). | n/a (brand-string sweep; recorded here) | brand sweep |
| `package.json` (`version`), `product.json` (`onboardingKeymaps` label + `defaultChatAgent.*Url` links) | Alpha versioning + links: `version` -> `1.125.0-alpha1` (carried only through the alpha phases, dropped at full release); the keymap-picker label "VS Code" -> "Clawdius Default (VS Code)"; all `clawdiuscode.io` placeholder links -> `https://github.com/chapmanjw/clawdius` (no website yet). | n/a (product / version) | product / release |
| `welcomeGettingStarted/browser/gettingStarted.ts` (welcome left/right columns) | Welcome page Clawdius sections (Clawdius mode only, `!defaultChatAgent.entitlementUrl`): (1) a permanent left-column **"Control Center"** section with one deep-link per Control Center tab (Usage / Permissions / MCP Servers / Skills / Plugins / Hooks), each dispatching `clawdius.openControlCenter` with the tab id through a new `selectControlCenterTab` case; (2) a **"New Claude Code Session..."** entry prepended to the **Start** list (Claude mark icon) that runs the official plugin's `claude-vscode.editor.open` ("Open in New Tab") so the session opens as an editor tab, not the sidebar - injected in code (not the static `startEntries` table) and resolved via a `clawdiusStartCommands` fallback in the `selectStartEntry` dispatch; (3) **Recent moved under Walkthroughs** in the right column (the left column is now Start + Control Center). All three verified live via Playwright. | `// CLAWDIUS-BEGIN permanent Control Center welcome section ...` | welcome / getting-started |
| NEW `browser/control/claudePluginsModel.ts` (+ test) + `browser/control/claudeControlCenterEditor.ts` (Plugins tab) | Control Center **Plugins** tab gains marketplace management + a browsable catalog (parity with the official plugin's `/plugin`). New pure model parses the local `~/.claude/plugins/` JSON (`known_marketplaces.json`, `installed_plugins.json`, each `marketplaces/<name>/.claude-plugin/marketplace.json` catalog). Tab is **Installed plugins** (enable/disable toggles; an "Add plugin" header button reveals add-by-id + a searchable Browse-marketplaces list with per-plugin Install) then **Marketplaces** (list with Update/Remove; an "Add marketplace" header button reveals a source input). **Zero-egress:** every read is a local file; every NETWORK action (`claude plugin install` / `marketplace add|update|remove`) is handed off to a visible integrated terminal via `sendText(cmd, false)` (review-before-run) - the IDE makes no network calls. Shell-safety: plugin ids gated by `PLUGIN_ID_RE`, marketplace names by `MARKETPLACE_NAME_RE`, add-source rejects newlines + never auto-runs. Search box re-focuses + restores the caret across the filter re-render. Verified live via Playwright. | `// CLAWDIUS-BEGIN` (both files) | clawdius / control center |
| `src/vs/workbench/browser/parts/globalCompositeBar.ts`, `src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarActions.ts`, `src/vs/workbench/browser/actions/layoutActions.ts`, `src/vs/workbench/services/actions/common/menusExtensionPoint.ts` | Editor/activity-bar UI polish batch (Clawdius mode). `globalCompositeBar.ts`: the bottom-left Accounts activity-bar button (which already opens the Control Center) now wears that identity — the `dashboard` codicon + a "Claude Code Control Center" hover — instead of the stock person icon + "Accounts" (reads the `productService` constructor PARAMETER pre-`super()`; one edit covers the titlebar `SimpleAccountActivityActionViewItem` variant too). `auxiliaryBarActions.ts` + `layoutActions.ts`: the "Toggle Claude Code Chat" top-bar toggle **and** its Customize Layout quick-pick entry now render a bespoke masked chat SVG (a `registerIcon` ThemeIcon painted via a CSS `mask-image` + `background-color: currentColor`, so it themes light/dark from one SVG) instead of the flat `Codicon.claude` glyph — supersedes the `Codicon.claude` toggle icon in the two rows above. `menusExtensionPoint.ts`: in the menus handler, SUPPRESS the anthropic.claude-code extension's own editor/title buttons (`claude-vscode.editor.openLast` / `.terminal.open`, scoped to that extension id + Clawdius mode) so the fork's own contiguous editor-title cluster (Open Chat → focus the right side-bar chat, New Session → `claude-vscode.editor.open`, New Terminal → an editor-area `claude` terminal) replaces them without a duplicate button or interleaving. All Clawdius-gated (empty `entitlementUrl`); upstream unchanged. NEW sibling files (not diff surface): `contrib/clawdius/browser/{clawdiusCustomIcons.ts, clawdiusEditorTitleActions.ts, clawdiusStartupInstallOverlay.ts}` + `media/{claude-code-{chat,session,terminal}.svg, clawdiusCustomIcons.css, clawdiusStartupModal.css}` — the masked icon set (base codicon + a knocked-out Claude mark), the editor-title cluster (gated on the `clawdius.claudeCodePluginInstalled` presence key + `!config.claudeCode.useTerminal`), and a non-blocking, dismissible first-run plugin-install overlay (reuses the Control Center's animated Clawd hero). Also the effort status pill's icon in the existing `contrib/clawdius/browser/clawdiusEffortStatusEntry.ts` swapped `$(dashboard)` → `$(chip)`. | `// CLAWDIUS-BEGIN account button presents the Control Center identity`, `// CLAWDIUS-BEGIN Claude chat-bubble toggle icon`, `// CLAWDIUS-BEGIN secondary side bar is the native Claude chat`, `// CLAWDIUS-BEGIN suppress the claude-code extension's editor-title` | workbench layout / activity bar / editor title / menus |
| `src/vs/base/browser/ui/iconLabel/iconLabels.ts` | `renderLabelWithIcons` (the IDE-wide label hot path) gains an opt-in per-cell split so the Clawdius effort/usage status-bar meter can colour/animate individual glyphs. A meter label encodes an animated run as `<NUL><className><NUL><chars><NUL>` (NUL = U+0000, never in a real label); `clawdiusPushText` emits each char as its own `span.clawdius-meter-cell <className>` with a `fill`/`empty` state class + a `--clawdius-i` index var so CSS styles/animates cells individually (malformed runs fall through verbatim). Guarded by one up-front `indexOf`: any label without the sentinel — every ordinary label IDE-wide — takes the EXACT original verbatim `elements.push(segment)` fast path, so the global hot path pays a single `indexOf` and no per-segment call. No `entitlementUrl` gate; a pure no-op for any label lacking a NUL. | `// CLAWDIUS-BEGIN per-cell animated meter cells` | base / icon label |
| `src/vs/base/common/product.ts` | Extension-management policy: added two optional `IProductConfiguration` fields. `clawdiusBlockedExtensions` (ids Clawdius refuses to install — conflicting AI assistants it ships a replacement for, e.g. GitHub Copilot; hard-enforced at the platform install chokepoint `checkAndGetCompatibleVersion`, so CLI/sync/gallery installs are blocked, and surfaced in the UI via the extensions-control manifest's `deprecated` map with `disallowInstall`) and `clawdiusUninstallProtectedExtensions` (ids the user can't uninstall — the Claude Code plugin Clawdius depends on; hard-enforced at the platform uninstall funnel `uninstallExtensions` covering UI/CLI/IPC/pack expansion, and the workbench Uninstall action is disabled for them). Additive optional type declarations only — no `entitlementUrl` gate here; both unset in upstream `product.json`, so upstream behavior is unchanged. | `// CLAWDIUS-BEGIN extension-management policy lists` | product config / types |
| `src/vs/platform/agentHost/common/agentService.ts` | Two Clawdius-only methods added to the `IAgentHostService` IPC contract (`extends IAgentConnection`; proxied to the agent host process). `discoverMcpServerTools(serverName, workingDirectoryPath)` (#93): opens a short-lived Claude SDK session rooted at the working dir to connect to a configured MCP server and return its tools; user-initiated only (the Control Center "Load tool names..." action), returns a `disabled` result when the agent host is off, and self-caps so the renderer never hangs. `getUsageStats(homeDirPath)` (#94): aggregates the raw Claude transcripts under `<home>/.claude/projects` into always-current dashboard stats (incremental cache, only changed files re-parsed), returns `unavailable` when the agent host is off (caller falls back to the engine's stats-cache.json), zero egress (local reads only). The two supporting result-type imports (`IClaudeMcpToolDiscoveryResult`, `IClaudeUsageStatsResult`) are unmarked. | `// CLAWDIUS-BEGIN live MCP tool discovery (#93)`, `// CLAWDIUS-BEGIN transcript-derived usage stats (#94)` | agent host / service contract |
| `src/vs/platform/agentHost/electron-browser/localAgentHostService.ts` | Two additive Clawdius RPC surfaces on the renderer-side agent-host MessagePort client (`LocalAgentHostServiceClient`), each a `getDelayedChannel` proxy built unconditionally in the constructor plus a public method. (1) Live MCP tool discovery (#93): `discoverMcpServerTools()` returns `{status:'disabled'}` when the agent host is off (never auto-`_connect()`s from a config dropdown) and races READINESS, not the RPC — waits for the MessagePort client (30s cap) and on timeout returns `{status:'timeout'}` WITHOUT ever issuing the call, so a timed-out discovery can't later reach the MCP server behind the click-only egress contract; once ready the call is live (node service self-caps at 25s). (2) Transcript-derived usage stats (#94): `getUsageStats()` does the same readiness wait (local `~/.claude` file reads only, zero egress) and returns `{status:'unavailable'}` when disabled/timed out. Additive interface methods; no upstream/Copilot path touched (guards key off `isAgentHostEnabled`, not `entitlementUrl`). | `// CLAWDIUS-BEGIN live MCP tool discovery (#93)`, `// CLAWDIUS-BEGIN transcript-derived usage stats (#94)` | agent host / renderer |
| `src/vs/platform/extensionManagement/common/abstractExtensionManagementService.ts` | Extension-policy guards driven by two new `product.json` arrays (NO `entitlementUrl` gate — both are inert no-ops when the arrays are empty, so upstream behavior is preserved by the empty defaults). `installExtensions()` — the common task-creation chokepoint, covering gallery, VSIX `install(vsix)`, and local `installFromLocation` alike — throws `ExtensionManagementError`/`Unsupported` ("can't be installed in {nameLong}") for any id in `productService.clawdiusBlockedExtensions`, refusing conflicting AI assistants (e.g. GitHub Copilot); the id is the gallery identifier or, for a VSIX/local URI, derived from the parsed manifest publisher+name. `uninstallExtensions()` reads `productService.clawdiusUninstallProtectedExtensions` and throws `Unsupported` ("required by {nameLong} and cannot be uninstalled") from INSIDE `createUninstallExtensionTask` (placed there so extension-pack members appended after the initial batch are also caught), protecting the Claude Code plugin Clawdius depends on; `uninstall()` funnels here so UI/CLI/IPC/remote are all covered. | `// CLAWDIUS-BEGIN refuse install of policy-blocked extensions ...`, `// CLAWDIUS-BEGIN refuse to uninstall a protected extension ...`, `// CLAWDIUS-BEGIN every uninstall task ... must spare the protected extension` | extension management |
| `src/vs/platform/extensionManagement/common/extensionGalleryService.ts` | Conflict-extension block: in `getExtensionsControlManifest()`, loop over `productService.clawdiusBlockedExtensions` and set each id `{ disallowInstall: true }` in the returned `deprecated` map (the same mechanism upstream uses one block above for the defaultChatAgent chat extension), so extensions that would compete with the fork's own Claude chat for the chat surfaces (e.g. GitHub Copilot) are refused at `canInstall()`/`checkAndGetCompatibleVersion()`. Deliberately NOT the `malicious` list, to avoid labelling a legitimate extension as malicious. Guarded by the list itself (`?? []`), not by `entitlementUrl`: a no-op when `clawdiusBlockedExtensions` is absent/empty, so the upstream path is unchanged in non-Clawdius builds. | `// CLAWDIUS-BEGIN refuse install of conflicting extensions` | extension management / gallery |
| `src/vs/platform/extensionManagement/node/extensionManagementService.ts` | Default extension signature verification OFF when the setting is unset: in `downloadExtension`, `verifySignature = isBoolean(value) ? value : false` instead of forcing verification on an unset `extensions.verifySignature`. The gallery is pinned to Open VSX, which does not MS-sign extensions, so an unset value threw "Signature verification was not executed" and blocked every install (including into remotes). An explicit `"extensions.verifySignature": true` still re-enables it. Unconditional (not `entitlementUrl`-gated); the systemic counterpart to the install-time `donotVerifySignature` note for `clawdiusPluginSetup.ts` in the pivot follow-ups. | `// CLAWDIUS-BEGIN default extension signature verification OFF` | extension management / signatures |
| `src/vs/server/node/serverServices.ts` | REH usage plumbing: registers two Clawdius channels on the remote-agent `socketServer` - the transcript-stats aggregator (`ClaudeUsageStatsService`) + the subscription-capacity fetcher (`ClaudeUsageCapacityService`) - so the Claude Code Usage dashboard works in WSL/SSH remote windows against the remote `~/.claude`; the renderer reaches them via `getConnection().getChannel(<name>)`. Registered UNCONDITIONALLY (independent of the agent host, which is NOT spawned on a standard remote, so behind no `--agent-host`/`--agent-host-bridge` gate). Stats reads local files only; capacity performs the single on-demand `/api/oauth/usage` fetch (zero uninitiated egress). Includes the paired import block. | `// CLAWDIUS-BEGIN usage dashboard channels (#94)` | server / remote agent |
| `src/vs/workbench/contrib/extensions/browser/extensionRecommendationsService.ts` | Suppress the "install the recommended extensions for this repository?" workspace prompt in Clawdius: `promptWorkspaceRecommendations()` early-returns (static `product` import) when `defaultChatAgent.entitlementUrl` is empty, before it queries local extensions or builds the allowed-recommendations list. Clawdius ships its own curated set (the official Claude Code plugin + the jeanp413 remotes via first-run setup) and leaves further extension choices to the user. Only the per-workspace nag is gated; the file/config/keymap/exe recommendation surfaces and the Extensions-view "Recommended" list are unchanged. Upstream prompt preserved when `entitlementUrl` is present. | `// CLAWDIUS-BEGIN no "install recommended extensions for this repository" prompt` | extensions / recommendations |
| `src/vs/workbench/contrib/extensions/browser/extensionsActions.ts` | Extensions viewlet: disable the Uninstall action for any extension id in `productService.clawdiusUninstallProtectedExtensions` (product.json: `anthropic.claude-code`), so the Claude Code plugin Clawdius depends on can't be uninstalled from the UI. `UninstallAction.update()` early-returns `enabled=false` on a protected id. UI companion to the service-layer guard in `abstractExtensionManagementService.ts` that blocks the uninstall op itself. Gated on the product array, not `entitlementUrl`, so it is inert upstream (the property is undefined there: `?? []` empty, `.some()` false, button unchanged). | `// CLAWDIUS-BEGIN protected extensions (the Claude Code plugin Clawdius depends on) cannot be uninstalled` | extensions / viewlet |
| `src/vs/workbench/services/agentHost/browser/editorRemoteAgentHostServiceClient.ts` | The renderer-side remote-agent-host `IAgentHostService` client (WSL/SSH path) implements the two Clawdius-added interface methods. (#94 usage stats) builds a `_usageStatsProxy` to the `ClaudeUsageStatsChannelName` channel the REH server registers on the remote-agent connection, wired unconditionally whenever a remote connection exists (the stats service is independent of the agent host, which is not spawned on a standard WSL/SSH remote), and `getUsageStats` races the delayed-channel call against a 30s timeout so a never-connecting remote degrades to `'unavailable'` instead of hanging - mirrors `localAgentHostService.getUsageStats`; reads the remote's local files only (zero egress). (#93 MCP discovery) `discoverMcpServerTools` is stubbed to return `status:'error'` since live MCP tool discovery is local-SDK-session only. Both are unconditional Clawdius-interface implementations - no `entitlementUrl` gate / no upstream-Copilot branch (neither method exists upstream). | `// CLAWDIUS-BEGIN transcript-derived usage stats (#94)`, `// CLAWDIUS-BEGIN live MCP tool discovery (#93)` | agent host / remote |
| `src/vs/workbench/services/extensions/common/abstractExtensionService.ts` | New public `restartRemoteExtensionHosts()` on `AbstractExtensionService`: `_initializeIfNeeded()`, then stops just the `ExtensionHostKind.Remote` host manager(s) via `_extensionHostManagers.stopOne()` and lets `_startExtensionHostsIfNecessary(false, ...)` recreate them, leaving the LOCAL host (and the remote-resolver extension it hosts, so the remote management connection survives) alive. Reuses the same stop-one+restart path upstream uses for remote-host crash recovery (`_onExtensionHostCrashed` for `ExtensionHostKind.Remote`); no-ops when no remote host exists. Purely additive new method — no upstream behavior changed, no `entitlementUrl` gate. | `// CLAWDIUS-BEGIN: restart only the remote extension host(s), leaving local hosts running` | extension service / hosts |
| `src/vs/sessions/browser/widget/openInVSCodeWidget.ts`, `.../contrib/applyCommitsToParentRepo/browser/applyChangesToParentRepo.ts`, `src/vs/workbench/contrib/agentsVoice/browser/components/sessionListComponent.ts`, `.../confirmationComponent.ts`, `src/vs/sessions/electron-browser/actions/vscodeActions.ts`, `.../contrib/policyBlocked/browser/sessionsPolicyBlocked.ts` | Brand-string sweep (prominent labels; same class as the row-86 display-text sweep): rebrand the six user-visible command/button/action LABELS that still said "VS Code" — the open-in-editor titlebar-widget hover ("...VS Code Editor Window" → "...Clawdius Editor Window"), the apply-changes "Open Folder" action label, the two Voice Mode confirmation buttons (`agentsVoice.openInVSCode`), the "Open VS Code Window" command-palette title (`agents.openVSCodeWindow`), and the policy-blocked "Open VS Code" button — all → "Open [in] Clawdius". Only `localize()` message text changed; keys, command ids, and the `vscode`/`vscode-insiders` URL schemes are untouched. NOTE: the DEEP class (~54 `localize()` settings-descriptions / notifications / install-errors still mentioning "VS Code") is intentionally DEFERRED to a batched full-IDE brand pass around the 1.127.0 merge (per row 86); it is NOT rebranded here. | `// CLAWDIUS-BEGIN brand sweep (open-in-editor hover/label)`, `// CLAWDIUS-BEGIN brand sweep (voice-mode open-in-editor label)`, `// CLAWDIUS-BEGIN brand sweep (open-window command title)`, `// CLAWDIUS-BEGIN brand sweep (policy-blocked open-editor button)` | sessions + voice / brand |
| `src/vs/workbench/workbench.desktop.main.ts`, `src/vs/sessions/sessions.desktop.main.ts` | 1.127 merge: the new "Allow Remote Connections" tunnel host (shares an agent-host session over a Microsoft/GitHub-authenticated tunnel; depends on the IDE account Clawdius removes) is fully removed by commenting out its two side-effect imports, so its chat-input toggle, `registerAction2` toggles, and `TunnelHostService` never register. Zero egress, no account. | `// CLAWDIUS: ... tunnel host` (both files) | chat + sessions / remote |
| `src/vs/sessions/sessions.common.main.ts` | 1.127 merge: the sessions-window GitHub PR-polling contribution (`contrib/github/browser/github.contribution.js` - a `PollingContribution` that polls `api.github.com` to keep each session's PR model warm, plus Open-PR / CI-rerun-failed-jobs actions) is not registered; its side-effect import is commented out. No IDE account, zero egress. Also drops the deleted upstream `sessionLayout.contribution` import and keeps the workflows-view-moved note. | `// CLAWDIUS: GitHub PR polling ... not registered` | sessions / github |
| `src/vs/platform/agentHost/node/agentService.ts` (+ deleted `agentHostPullRequestOperationProvider.ts`, `agentHostPullRequestOperationHandler.ts` and their tests) | 1.127 merge: the new agent-host create-PR / enable-auto-merge changeset operation (`POST api.github.com/repos/.../pulls` + a GraphQL auto-merge mutation, agent-invokable) is removed entirely - the commit + pull-request operation registrations are dropped and their provider/handler files deleted; only the local-only sync + discard-changes operations remain. The on-turn and session-restore GitHub PR-lookup calls (`_gitStateService.attachSessionGitHubPullRequest`) are also dropped. Mirrors the pre-existing commit-op removal. | `// CLAWDIUS: the commit + pull-request changeset operations are removed entirely`, `// CLAWDIUS: upstream also called ...attachSessionGitHubPullRequest ... dropped` | agent host / github |
| `src/vs/platform/agentHost/node/claude/claudeAgent.ts`, `.../claudeSdkOptions.ts`, agent-host mains + starters, `browser/remoteAgentHostProtocolClient.ts` | 1.127 merge re-anchoring of existing gates: `getProtectedResources()` keeps `return []` and now DOMINATES a new upstream `return [GITHUB_REPO_PROTECTED_RESOURCE]` branch (this one line transitively closes 1.127's auto-PR-lookup egress); the agent-host OTEL env scrub was widened from the endpoint var to the whole `OTEL_*` prefix (+`COPILOT_OTEL_*`) because 1.127 added OTLP headers/protocol/serviceName/resourceAttributes; the proxy-handle -> `ClaudeTransport` refactor was reconciled to the fork's native no-proxy path (the proxy module stays deleted); and Codex (agent + module) was fully stripped from the retained agent-host files (Codex was removed in the fork; 1.127 re-introduced references). | existing `// CLAWDIUS-BEGIN` gate markers (native ~/.claude auth, zero-egress OTEL, no copilot agent) | agent host / security |
| ~50 files: `localize()` settings-descriptions / notifications / install-restart errors across platform (extension-management, update, terminal, agent-host), chat, terminal, sessions, welcome; the remote-CLI `server.cli.ts` output; the 4 terminal shell-integration activation-failure messages; Windows installer `build/win32/i18n/messages.*.isl`; the macOS Finder document "Kind" in `build/lib/electron.ts`; built-in extension `package.nls.json` / `package.json` (settings text rebranded, `displayName`/`description` neutralized); built-in extension RUNTIME `.ts` `vscode.l10n.t()` strings (media-preview open-file links, typescript/html-language-features notifications + error dialogs, configuration-editing completions, extension-editing lint); the browser-workbench `callback.html` title/branding; and the OAuth/loopback sign-in pages (`microsoft-authentication` `src/node/loopbackTemplate.ts` + `media/index.html`, and the core-workbench GitHub-auth `src/vs/workbench/api/node/loopbackServer.ts` — product name -> Clawdius, branding href repointed from `code.visualstudio.com` to the fork repo); and the exported enterprise Group Policy data `build/lib/policies/policyData.jsonc` (the checked-in artifact the build consumes: the Claude/Codex agent-policy + MCP-registry descriptions rebranded and their false Copilot-subscription claim dropped, hand-synced to the already-clean source since the exporter reads compiled output — the stale Codex-agent policy ENTRY itself is a separate Codex-removal follow-up); the `code --help` + zsh-completion telemetry-flag description, the macOS `darwinHelpBook*` plist name, the css/html custom-data schema hovers, and the MCP-gateway / renderer-debug `serverInfo`/CDP display names (this class included a lowercase "VS code" case-variant that case-sensitive sweeps miss) | The deferred deep-class display-text brand sweep for the alpha (classified + two-voice-reviewed on both input scope and output diff, then applied): user-visible product self-references "VS Code" / "Visual Studio Code" -> "Clawdius" in `localize()` / `l10n.t()` TEXT/VALUES and the equivalent shipped non-localized strings; also dropped a false "Uses your existing Copilot subscription" claim in the Claude-Agent policy description. Extension `displayName`/`description` use NEUTRAL wording ("Grunt support", not "for Clawdius"). UNTOUCHED (KEEP): the `vscode:` URL scheme, `vscode.*` API namespace, `engines.vscode`, `vscode:prepublish`/`uninstall`, `aka.ms/vscode-*` + `code.visualstudio.com` URLs, `localize()` keys, command/setting ids, the agent-file `target: vscode` format vocabulary + its validator messages, the "VS Code Synced Data" protocol identity, the "VS Code Speech" extension proper noun, `TERM_PROGRAM=vscode`/`VSCODE_*`/`__vsc_*`, the suppressed "Get started with VS Code" Setup walkthrough, the `extensionLinter.ts` "reach out to the VS Code team" upstream API-proposal governance reference (a real external team), and comments/JSDoc/tests. The 5 most prominent rebrands are pinned in `branding-guard.ts`. | `// CLAWDIUS-BEGIN brand sweep (...)` (+ `#` for shell scripts, `;` for `.isl`; JSON files carry no inline marker) | brand sweep |

## Branding asset replacements (in-place upstream binary / SVG swaps)
The VS Code logo/mark assets, replaced in place with the Clawdius mark (orange `#d97757` family,
re-exported from the Clawdius logo master). These are upstream files, so a
merge that touches the same asset conflicts; re-export from the Clawdius master rather than hand-merge.
- **OS app-icon family:** `resources/win32/code.ico` + `code_150x150.png` + `code_70x70.png`,
  `resources/darwin/code.icns`, `resources/linux/code.png`, `resources/linux/rpm/code.xpm` (256px),
  `resources/server/code-192.png`, `code-512.png`, `favicon.ico`. `linux/code` + `server/code-192/512`
  share one 1024² master (byte-identical, retina-correct).
- **Inno installer wizard art:** `resources/win32/inno-{big,small}-{100,125,150,175,200,225,250}.bmp`
  (14 BMPs) — Clawdius logo centered on white at each placeholder's footprint size.
- **In-product / chrome logos:** `src/vs/workbench/browser/media/code-icon.svg` (titlebar/banner/welcome),
  `src/vs/sessions/browser/media/vscode-icon.svg` (Open-in-VS-Code), and `sessions-icon.svg`
  (Open-in-Agents widget; blue gradient recolored to the orange family).
- **Auth login chrome (interim, until a later pass removes the flow):**
  `extensions/github-authentication/media/favicon.ico` + `code-icon.svg`,
  `extensions/microsoft-authentication/media/favicon.ico`.

## New files / directories (no conflict, not part of the diff surface)
`UPSTREAM_VERSION`, `MERGING.md`, `CHANGES_AGAINST_UPSTREAM.md`, `BUILD.md`, `.gitleaks.toml`,
`.pre-commit-config.yaml`, `script/clawdius/**`, `clawdius/**`, `.github/workflows/clawdius-ci.yml`,
`extensions/clawdius-themes/**`, `extensions/clawdius-chat/**` (default chat participant, publisher `vscode`, backed by the local Claude Code CLI; id `vscode.clawdius-chat`),
`src/vs/workbench/services/accounts/test/browser/defaultAccount.test.ts` (Clawdius regression test).

## On-demand egress (user-initiated, never at startup/idle)
- The Claude Code usage pane shows live rate-limit "capacity" bars. The clawdius-chat extension (node, no
  CORS - the renderer can't reach the API) fetches `GET https://api.anthropic.com/api/oauth/usage` using the
  user's existing CLI OAuth token (`~/.claude/.credentials.json`) and caches it to
  `~/.claude/.clawdius-usage-cache.json`; the core status entry reads that cache. This fetch runs **on demand
  only** - the status entry executes the `clawdius.refreshUsageCapacity` command when the user opens the
  usage tooltip (`claudeUsageEntry.ts` `refreshOnDemand`); there is **no startup fetch and no 60s background
  poll** (removed), so a Clawdius install makes zero uninitiated network egress per the guarantee. The bars
  populate the first time the user looks at them; the status entry's 15s poll is LOCAL-file reads only.
  (Was an `activate()` fetch + 60s `setInterval`; changed to on-demand after the egress audit.)

## Known privacy follow-ups (tracked, deferred by design — not yet addressed)
Surfaced by the privacy review; recorded so the zero-egress claim stays honest about what is and is not done.
- `product.json` `webviewContentExternalBaseUrlTemplate` still points at `*.vscode-cdn.net`. Verified this
  is **not** a desktop egress vector: desktop webviews use the local `vscode-webview://{{uuid}}` scheme
  (`environmentService.ts` electron-browser); `vscode-cdn.net` appears only in the web/server build CSP
  (`webClientServer.ts`) and the remote-resource URI transform. Re-point to a Clawdius-controlled host
  before shipping the web/REH build.
- `product.json` `defaultChatAgent` still carries Copilot-specific command/output identifiers
  (`chatExtensionOutputId`, `chatExtensionOutputExtensionStateCommand`, the `github.copilot.*` commands /
  contexts / settings) and a GitHub auth provider block. These are non-URL identifier strings with no
  egress sink. The startup contribution that *consumes* the provider block (`DefaultAccountProviderContribution`)
  is now disabled while `entitlementUrl` is empty (see the `defaultAccount.ts` edit above), so the provider
  data is inert for now. Replace the identifiers and define the real auth model with the Clawdius chat
  extension in a later pass.
- `product.json` documentation/terms/privacy/plan/signup URLs were repointed from the `clawdiuscode.io`
  placeholders to `https://github.com/chapmanjw/clawdius` (see the alpha-versioning row), so no unregistered
  domain ships; revisit if a dedicated site is ever stood up.

## Discipline
Every future in-place edit is wrapped in `// CLAWDIUS-BEGIN <reason>` / `// CLAWDIUS-END` (or the
comment syntax of the file), kept minimal, and added to the table above with the file, reason, and
upstream area touched. Prefer data (`product.json`) and new files over in-place edits.

## Rich chat + retire old chat (in-place upstream edits)
- `src/vs/workbench/contrib/chat/browser/chatParticipant.contribution.ts` (in-place, marked): the old workbench chat container (`workbench.panel.chat`) is `isDefault: clawdiusOldChatIsDefault` (= `!!product.defaultChatAgent?.entitlementUrl`) so in Clawdius the NATIVE webview Claude chat (`CLAWDIUS_CHAT_VIEW_CONTAINER_ID`, flipped to `isDefault:true`) owns the right pane. The old chat stays REGISTERED (workbench services hard-reference `ChatViewId`; full unregistration is unsafe) - it is just non-default + a fallback. Adds a `product` default import.
- `src/vs/workbench/contrib/chat/electron-browser/chat.contribution.ts` (in-place, marked): the Ultracode/Agents window LAUNCHERS (`OpenWorkspaceInAgentsWindowAction`, `ToggleOpenInAgentsWindowTitleBarAction`, `OpenAgentsWindowAction`, `OpenChatSessionInAgentsWindowAction`, the `OpenWorkspaceInAgentsContribution` banner + `AgentsHandoffInputTipContribution`) are gated behind `if (product.defaultChatAgent?.entitlementUrl)` so they do not register in Clawdius - removing the command-palette commands, keybinding, title-bar button, and banner. The sessions/Ultracode WINDOW RUNTIME (`src/vs/sessions/**`, the `--agents` path, `openAgentsWindow` service) is LEFT INTACT because its config/customization panels are not yet ported to the main-IDE left container.
- `src/vs/workbench/contrib/clawdius/browser/chat/clawdiusChatViewPane.ts` (new file, extended): rich rendering - per-part assistant blocks (markdown via a CSP-safe string-method renderer, collapsible thinking, tool cards with inline Allow/Deny permission prompts dispatched as `SessionToolCallConfirmed` approve/deny). Survives container switches via an `IOverlayWebview`.

### Deferred (morning) - tracked honestly, NOT yet done
- Path-A neutralization (revert `chat.editor.defaultProvider` `claudeAh`->`local` in `agentHost.config.contribution.ts`; remove the `claudeAh`->`SessionType.AgentHostClaude` branch in `constants.ts`; remove the untitled fallback in `chatEditorInput.ts`). Deferred to avoid an unsupervised 3-file config change overnight; low impact since the old chat is now non-default.
- Move `Ctrl/Cmd+Alt+I` from the old chat to the Clawdius chat (the keybinding strip edit fought file indentation).
- Remainder: tool `PendingResultConfirmation` result-approval gate (needs `SessionToolCallResultConfirmed`), live tool `content` (terminal output), TodoWrite checklist, inline diffs + accept/reject, model picker, @-mention, /-slash, plan/accept modes, cost meter. Thinking block re-collapses on each streaming delta (rebuild loses `<details>` open state).
- Full Ultracode window removal after its config/customization panels are ported to the left Clawdius container.

## Chat feature build-out
All chat work is in the marked Clawdius files (`clawdiusChatViewPane.ts`, new `common/clawdiusChatTodos.ts` +
its test) plus two in-place gates. Each feature was reviewed and its
findings fixed before the feature was considered done.

- `src/vs/platform/agentHost/node/claude/claudeSdkOptions.ts` (in-place, marked): SECURITY fix.
  `allowDangerouslySkipPermissions` was hardcoded `true`, silently auto-approving every tool. Now set only for
  `permissionMode === 'bypassPermissions'`. Per the SDK contract (`sdk.d.ts`) `dontAsk` means "don't prompt,
  DENY if not pre-approved" (auto-deny), so it must NOT skip-and-run - review caught that an earlier
  `|| dontAsk` re-opened the hole. default/acceptEdits/plan/auto/dontAsk all defer to canUseTool.
- `src/vs/workbench/contrib/chat/browser/agentSessions/experiments/agentSessionsExperiments.contribution.ts`
  (in-place, marked): the title-bar agent-status badges + CommandCenter/TitleBar menu items are gated behind
  `if (product.defaultChatAgent?.entitlementUrl)` so they do not register in Clawdius (no stale notifications).
- `src/vs/workbench/contrib/clawdius/browser/chat/clawdiusChatSessionService.ts` (new): window-scoped service
  owning the Claude session URI/lifecycle so the conversation survives the pane being disposed/recreated. The
  ViewPane subscribes/dispatches but never creates/disposes it.
- `src/vs/workbench/contrib/clawdius/browser/chat/clawdiusChatViewPane.ts` (extended): full-transcript render
  from authoritative SessionState on each reattach; empty-successful-turn fallback; **model picker** (header
  dropdown, `SessionModelChanged`, preserves thinkingLevel config across a switch); **TodoWrite live checklist**
  (collapses a turn's repeated updates to the latest committed list; pending-confirmation TodoWrite keeps its
  Allow/Deny; never swallows an empty/malformed card); **tool text output** (Running/Completed/
  PendingResultConfirmation, content-only extractor, capped at 10k chars); **token usage meter** (current
  turn's tokens, hidden during a fresh stream); **file-edit summaries** (path + added/removed lines on the pending
  permission card and completed results; only deletion asserted from before/after presence); **inline
  completions / @-mention / slash** (agent-host `completions()` + `getCompletionTriggerCharacters()`,
  keyboard-navigable dropdown, span-tracked attachments threaded into the turn with edit-delta adjustment +
  identity dedupe + stale-response guard).
- `src/vs/workbench/contrib/clawdius/common/clawdiusChatTodos.ts` (new) + `test/common/clawdiusChatTodos.test.ts`
  (new): pure TodoWrite parse + projection helpers, unit-tested (parser robustness, live-id selection, render
  classification incl. the never-swallow guard).
- `clawdiusChatViewPane.ts` (continued): **thinking-intensity control** (a second header dropdown for the
  current model's first string-enum `configSchema` property, dispatches `SessionModelChanged` with merged
  config); **approvals / plan-mode control** (a header dropdown for the session's first mutable string-enum
  config property -- `permissionMode`: default/acceptEdits/plan/etc. -- driven by `SessionConfigState.schema`
  + `.values`, dispatches `SessionConfigChanged`; both config dropdowns share one `renderConfigSelect`).
- `clawdiusChatViewPane.ts` + new `common/clawdiusChatDiff.ts` (+ test): **inline file-edit diffs** (POST-APPLIED).
  `_kickToolDiffs` resolves each FileEdit's before/after `ContentRef` (`resourceRead`, Utf8 only), computes a
  compact unified line diff (`computeLineDiff` / LcsDiff, capped 400 lines, unit-tested), and posts it per
  tool-call id; the SPA renders +/-/context lines under the card. Cache retries on empty/failure so the
  Completed-state diff always lands. Allow/Deny IS the Claude accept/reject gate (no separate
  changeset accept/reject -- changesets are commit/PR-only).

### Still deferred after this session (tracked honestly, NOT done)
- **Pre-approval diff PREVIEW**: Claude's `canUseTool` pending confirmation carries NO
  `edits`; Claude stages diffs only AFTER the tool runs (`ClaudeFileEditObserver`). So the inline diff shows
  POST-application, and Allow/Deny is currently answered WITHOUT a diff preview. The SDK `canUseTool` gives only
  the raw tool input (verified), so a preview means reimplementing Edit/Write/MultiEdit apply at canUseTool in
  `claudeCanUseTool.ts` (compute after-content, stage before/after refs, populate `state.edits`). This touches
  the PERMISSION path and feeds the approval decision -- deliberately deferred to a fresh context to avoid a
  misleading preview or a half-applied change breaking Allow/Deny.
- Image paste into the composer (clipboard image -> binary `MessageAttachment`). Medium-large.
- Path-A config neutralization and the `Ctrl/Cmd+Alt+I` keybinding swap (from the night session).
- `PendingResultConfirmation` result-approval gate: Claude's mapper emits
  `SessionToolCallComplete` without `requiresResultConfirmation`, so this state is currently dead code for the
  Claude host (the tool-output render handles it informationally).
- The completion state machine runs as inline webview SPA string-JS; it cannot be unit-tested without a
  browser harness, so its correctness is gated by review. Extract the pure span/diff helpers to a tested
  module if it grows.

## PIVOT: adopt the official Anthropic plugin (SUPERSEDES all the bespoke chat above)
The bespoke webview chat was the wrong approach. The goal is the REAL Anthropic "Claude Code" plugin pane
(its own 5 MB webview-view in the secondary sidebar), installed and configured -- not a reinvention, and not
VS Code's Copilot chat. The official `anthropic.claude-code` is on Open VSX (v2.1.186), and Clawdius's
`product.json` gallery already points at Open VSX, so we install it on first run.

- **RETIRED** (deleted): the entire bespoke webview chat -- `browser/chat/` (clawdiusChatViewPane,
  clawdiusChatSessionService, clawdiusChat.contribution), `common/clawdiusChat{Todos,Diff}.ts` + their tests,
  the `workbench.common.main.ts` import, and the chat-only view IDs in `common/clawdius.ts`. Everything in the
  two chat sections above are SUPERSEDED -- those features now come from the real plugin.
- **NEW** `src/vs/workbench/contrib/clawdius/browser/clawdiusPluginSetup.ts` (registered from
  `clawdius.contribution.ts` at `WorkbenchPhase.AfterRestored`, Clawdius-mode only, storage-flagged once):
  installs `anthropic.claude-code` from the configured gallery (Open VSX) via `IExtensionsWorkbenchService`
  if not already present, then sets `claudeCode.preferredLocation=sidebar`, `useTerminal=false`,
  `hideOnboarding=true` via `updateValue(USER)` -- only for keys the user has not overridden -- and shows a
  one-time "ready / Configure engine" notification. Engine = the plugin's bundled `claude.exe` (225 MB) +
  the user's `~/.claude` auth; `claudeProcessWrapper` left unset (the field WRAPS, not replaces, the bundled
  CLI, so it cannot cleanly point at a PATH claude -- confirmed against `clawdiusCliSpawn.ts`). Failures are
  non-blocking and retried next launch.
- **KEPT** (unchanged): `extensions/clawdius-chat` + the `defaultChatAgent` product contract -- it is the
  Clawdius-mode sentinel (~130 core paths gate on `!entitlementUrl`; several read it unguarded; core marks it
  uninstall-protected), renders no UI of its own, and removing it is a multi-file refactor with high
  regression risk for zero benefit. The agent-host (`platform/agentHost/**`) + `IClawdiusCliConfigService`
  (#54) + workflows view + branding + all Copilot-suppression gates stay.
- **Copilot chat**: already suppressed by the existing gates (`workbench.panel.chat` non-default + hidden view
  + `hideIfEmpty`); the official plugin's secondary-sidebar pane owns the visible chat. The Clawdius
  secondary-sidebar toggle labels ("Toggle Claude Code Chat" + claude icons) now correctly describe the
  plugin's pane -- no change needed.

### Pivot follow-ups (verify in a real build)
- Open VSX signature verification may reject the auto-install in a BUILT build; if so, add
  `donotVerifySignature: true` to the `install` options (Open VSX is not signed like the MS marketplace). Noted
  inline in `clawdiusPluginSetup.ts`.
- Confirm `disableLoginPrompt`/auth behaves for a logged-in `~/.claude` (no spurious prompt); set it if needed.
- A `claudeProcessWrapper` -> PATH-claude override (a Clawdius launcher that discards the bundled-binary arg)
  is an advanced, live-test-required follow-up -- NOT the default.

## Claude Code usage surfaces + welcome-art letterpress (status bar, hover, dashboard)

### Letterpress watermark
- **CHANGED** `browser/parts/editor/media/letterpress-{dark,light,hcDark,hcLight}.svg`: the empty-editor
  watermark now uses the official Claude Code welcome-art (extracted from anthropic.claude-code) instead of
  the square Copilot mark. dark + hcDark <- welcome-art-dark.svg; light + hcLight <- welcome-art-light.svg.
- **CHANGED** `editorgroupview.css` `.letterpress` (marked): aspect-ratio 1/1 -> 440/189 and max-width
  256 -> 340px so the wide art is not boxed into dead vertical space.

### Usage status bar + hover + dashboard (replaces the old claudeUsageEntry)
- **NEW** `contrib/clawdius/browser/usage/`:
  - `claudeUsageData.ts` -- shared types, local-only readers (stats-cache.json with shape-normalization +
    a daily-activity cap; `.clawdius-usage-cache.json` capacity + account fields; settings.json provider
    detection => Bedrock/Vertex/custom hide limits), helpers, and the Claude wordmark as an inline SVG that
    inherits `currentColor`. Command ids for the user-initiated capacity refresh + the dashboard-open command.
  - `claudeUsageStatusEntry.ts` -- status-bar indicator: Claude logo (text-colored) + one inline horizontal
    session (5h) bar, hidden for non-Anthropic providers. Hover popup: account header + horizontal
    Session/Weekly bars (Copilot-style) + "Open Usage Dashboard" CTA; re-renders in place when the cold-cache
    on-demand fetch lands. Click opens the dashboard. Opening the hover is the sole, user-initiated trigger
    for the one allowed /api/oauth/usage refresh; the 15s poll is LOCAL-only.
  - `claudeUsageDashboardInput.ts` + `claudeUsageDashboardEditor.ts` -- a native DOM EditorPane (no webview,
    so trivially zero-egress) rendering: account hero band (clawd.svg + email/plan/provider/auth + privacy
    note + Refresh), subscription-limits panel (hidden for non-Anthropic providers), all-time usage cards,
    model mix, activity heatmap + daily trend. `setInput` only READS local files (a restored editor never
    egresses at startup); the live refresh is driven by the open action + the Refresh button, lifecycle-bound
    via a CancellationTokenSource. NO cost anywhere -- usage + limits only.
  - `media/claudeUsage.css` + `media/clawd.svg`.
  - Registered in `clawdius.contribution.ts` (Clawdius-mode gate): the status entry (BlockRestore), the
    editor pane + serializer, and the `clawdius.openUsageDashboard` command (Action2; refreshes capacity
    user-initiated, then opens/reveals the dashboard).
- **CHANGED** `browser/parts/globalCompositeBar.ts`: in Clawdius mode a left-click / Enter on the bottom-left
  Account button opens the usage dashboard (which carries the account identity) instead of the accounts popup;
  right-click still shows the account menu. Base `run()` made `protected`; the command id is referenced by
  string (not imported) to avoid a `browser/parts` -> `contrib` layer violation.
- **RETIRED** (deleted): `contrib/chat/browser/chatStatus/claudeUsageEntry.ts` + its import/registration in
  `chat.shared.contribution.ts` (superseded). The AI-stats suppression comment in `aiStatsStatusBar.ts` now
  points at the new path.
- Design synthesized from claudometer / ClaudeCodeUsageTracker / claude-usage-bar (Claude evaluation),
  dropping ALL cost/pricing/budget features per the usage-and-limits-only constraint. A review of the
  implementation: no uninitiated egress, account hook safe, no secret leak; its 6 findings (open-action
  refresh, cold-hover re-render, JSON shape-guarding, activity cap, dispose-race, focusable pane) are fixed.

### Usage follow-ups
- Bounded incremental JSONL indexer (`~/.claude/projects/*/*.jsonl`) for the richer dashboard sections
  (24h-of-day activity, tool frequency, per-project/session, cache read/write split) -- keyed by
  path+size+mtime+offset so the hover never triggers it and large histories are not rescanned; built only on
  dashboard open. NEXT.

## Release-prep repo hygiene (2026-06)

A public-release hygiene pass; all changes are committed (see git log). Grouped by kind.

### In-place edits to upstream files
- `eslint.config.js`, `build/hygiene.ts`: added a Clawdius copyright-header variant for Clawdius-authored dirs (`contrib/clawdius`, `platform/clawdius`, `clawdius-chat`). Both enforcers still require the Microsoft header elsewhere; only the copyright line differs. Marker `// CLAWDIUS-BEGIN clawdius-authored ... header`.
- `build/lib/electron.ts`, `build/gulpfile.vscode.ts`, `build/gulpfile.reh.ts`, `cli/build.rs`: Win32 version-resource publisher fields -> CompanyName "John Chapman", LegalCopyright "Copyright (C) 2026 John Chapman; portions Copyright (C) Microsoft Corporation". The MIT per-file headers are untouched.
- `cli/Cargo.toml` + renamed dir `cli/src/bin/code` -> `cli/src/bin/clawdius`, `build/azure-pipelines/cli/cli-compile.yml`: Rust CLI bin `code` -> `clawdius` (internal crate `code-cli` and lib `cli` unchanged). NOTE: the fork does not currently build/ship the Rust CLI (CI is GitHub Actions, not the upstream Azure pipeline), so this is source-consistency only.
- `build/npm/postinstall.ts`: the `.claude/CLAUDE.md` symlink repointed to the renamed dev guide.
- `test/{README, unit/README, integration/browser/README, smoke/README, sanity/README, automation/README, mcp/README}.md`: full Clawdius rebrand (the 9 code.visualstudio.com links repointed to chapmanjw/clawdius).
- `.github/ISSUE_TEMPLATE/{bug_report, config, feature_request}.md`, `.github/pull_request_template.md`: rebranded to the fork; dead Microsoft-community links dropped.
- `.vscode-test.js`: dropped the dead `copilot` integration suite. `.eslint-allowed-javascript-files`: pruned 36 entries for removed copilot/lit-html/signals-core files; normalized to LF. `.vscode/settings.json`: dropped the dead azure-pipelines autoApprove ref.
- `CHANGES_AGAINST_UPSTREAM.md`: reconciled 4 stale spots (retired claudeUsageEntry.ts in the new-files list, superseded rows 35-37, row 67 restricted-imports), plus this section.

### Renamed
- `.github/copilot-instructions.md` -> `.github/clawdius-instructions.md` (the repo's Claude-agent dev guide; content rebranded). The product-wide `copilot-instructions.md` workspace convention (its enum + discovery + ~30 tests) is intentionally left unchanged.

### Removed upstream files (dead for this fork)
- Microsoft-internal CI/security: `.config/1espt/`, `.config/guardian/.gdnsuppress`.
- Dev container: `.devcontainer/` (its README cloned microsoft/vscode; the fork builds natively on Windows and packaging never reads it).
- GitHub bots/metadata: `.mention-bot`, `.github/CODEOWNERS`, `classifier.json`, `commands.json`, `similarity.yml`, `insiders.yml`, `endgame/insiders.yml`, `CODENOTIFY`, `commands/codespaces_issue.yml`, `ISSUE_TEMPLATE/copilot_bug_report.md`, `agents/{data,demonstrate}.md`, `instructions/{kusto,telemetry}.instructions.md`, `skills/azure-pipelines/`, and the Microsoft issue-triage prompts (`codenotify`, `build-champ`, `find-duplicates`, `find-issue`, `fixIssueNo`, `issue-grouping`, `migrate`). KEPT: `dependabot.yml`, the generic dev-guidance instructions/prompts/skills, and `.config/configuration.winget`.

### New convention
- 51 Clawdius-authored `.ts` files now carry `Copyright (c) 2026 John Chapman (Clawdius)`. Upstream-derived files keep the Microsoft header (MIT requires retaining it; `LICENSE.txt` already carries the dual notice).

### Moved out of the repo (not tracked)
- The gitignored receipts directory (never committed) was moved to a private docs location. Its gitignore line is kept for future receipts.

## Release pipeline (2026-06)

Tag-driven build/sign/publish for the desktop apps. New files plus in-place edits to the
packaging templates.

### New files
- `.github/workflows/clawdius-release.yml` - tag-triggered (v*) pipeline: Windows
  (x64/arm64), Linux (x64/arm64 deb/rpm/tar + best-effort snap), macOS (universal), then a
  publish job that drafts a GitHub Release with each leg's signed SHA256SUMS.
- `build/release/win32.ps1` - Windows build -> Azure Trusted Signing (guarded) -> Inno
  user/system installers -> zip -> checksums. Locally validated (x64, unsigned).
- `build/release/linux.sh` - Linux build -> tar/deb/rpm -> GPG sign (guarded) -> checksums.
  Authored; pending runner/WSL validation.
- `build/release/darwin.sh` - universal build -> Developer ID sign + notarize (guarded) ->
  dmg + zip. Authored; pending macOS runner validation.

### In-place edits to upstream packaging
- `package.json` / `package-lock.json`: version 1.125.0-alpha-0.1 -> 1.125.0-alpha1 (the
  old suffix broke the Inno RawVersion regex).
- `build/win32/code.iss`: AppPublisher Microsoft Corporation -> John Chapman; the
  code.visualstudio.com URLs + install-time MsgBox -> the fork; OutputBaseFilename
  VSCodeSetup -> ClawdiusSetup.
- `resources/linux/**`: rebranded the deb/rpm/snap/AppStream metadata to Clawdius/John
  Chapman/the fork, and REMOVED the Microsoft apt-repo + GPG-key registration from
  debian/postinst (a Clawdius .deb must not add packages.microsoft.com); debian/templates
  emptied.
- `build/darwin/sign.ts`: macOS permission-prompt strings (Camera/Mic/AppleScript/Audio
  Capture) "...in Visual Studio Code..." -> Clawdius.
- `build/darwin/create-dmg.ts`: DMG title defaulted to "Code OSS" -> product.nameLong;
  background falls back to None when the quality-specific tiff is absent.

### Known follow-ups
- A Clawdius dmg-background tiff (no background for the no-quality fork until added).
- arm64, snap, and the whole macOS leg are unproven until a runner pass.
- The operational signing/cert setup lives in the private docs repo, not here.

## Rebase onto VS Code 1.126.0 (2026-07)

Base moved microsoft/vscode `1.125.0` -> `1.126.0` (`UPSTREAM_VERSION`). `product.json` was byte-identical
between the two tags, so the branding/privacy overlay carried through untouched; most conflicts were
mechanical (the already-deleted `extensions/copilot/**` tree, the removed upstream CI). Grouped by kind.

### Agent-host de-Copilot: removed the vendored Copilot SDK type-deps
- `package.json`, `remote/package.json`, `eslint.config.js`: removed the three `import type`-only Copilot
  SDK packages the fork no longer uses - `@github/copilot`, `@github/copilot-sdk`, `@vscode/copilot-api` -
  as dependencies and from the ESLint restricted-import allowlist. They were type-only (no runtime
  require), so removal is a compile-surface change, not a behavior change.
- Deleted the now-orphaned agent-host `copilot` directory those type-deps fed. Generic helpers it
  exported that live agent-host code imported by RELATIVE path were relocated first (a `git grep` on the
  absolute import path misses `./copilot/` importers). One fixture test was re-expressed against the plain
  history-record model to drop its Copilot-SDK type import.
- `package-lock.json` + `remote/package-lock.json` regenerated for the dependency drop, with the `ssh2`
  `cpu-features` optional stub re-grafted afterward (Windows `npm install` prunes it; a Linux `npm ci`
  needs it - see `docs/MERGING.md`).

### In-place edits to upstream files
- `src/vs/platform/agentHost/node/agentHostMain.ts`, `.../agentHostServerMain.ts`: NEW zero-egress gate.
  1.126 adds an agent-host OpenTelemetry exporter (`AgentHostOTelService`) constructed UNCONDITIONALLY in
  both the desktop and server agent-host entrypoints, and it does not route through `ITelemetryService`,
  so `enableTelemetry:false` does not suppress it - a standard `OTEL_EXPORTER_OTLP_ENDPOINT` in the
  inherited env would make the first Claude turn POST traces (optionally including prompt/response text).
  Both the `createInstance` and service-set blocks are now gated on a non-empty
  `defaultChatAgent.entitlementUrl` (Clawdius empties it), mirroring the existing agent-registration gates;
  the Claude SDK subprocess env scrub also drops `OTEL_*` / `COPILOT_OTEL_*`. | `// CLAWDIUS-BEGIN no agent-host OTEL exporter in clawdius (zero egress)`
- `src/vs/workbench/contrib/extensions/browser/extensions.contribution.ts`: hide the upstream
  "MCP Servers" and "Agent Plugins" sections from the Extensions pane in Clawdius mode - Clawdius manages
  MCP servers and agent plugins in its own Control Center, so the built-in Extensions-viewlet sections are
  redundant. A `BlockRestore` guard removes them before the mcp/chat contribs (`AfterRestored`) register.
  | `// CLAWDIUS-BEGIN hide upstream "MCP Servers" and "Agent Plugins" sections from the Extensions pane`

### New Clawdius-mode UX (alpha prep)
- `src/vs/workbench/contrib/clawdius/browser/clawdiusHiddenSettings.ts` (new) + registration in
  `clawdius.contribution.ts`: hides a small set of inapplicable upstream chat settings from the Settings
  editor via an `included:false` re-registration (`chat.titleBar.signIn.enabled`,
  `chat.growthNotification.enabled`, `chat.agents.copilotCli.hideExtensionHost`,
  `chat.editor.copilotCli.hideExtensionHost`). Each key's only reader is a surface Clawdius already
  suppresses, so hiding them removes dead toggles without changing behavior - a value set in
  `settings.json` is still honored. Clawdius-mode only (empty `entitlementUrl`).
- `src/vs/workbench/contrib/clawdius/browser/clawdiusDisableAnimations.ts` +
  `media/clawdiusDisableAnimations.css` (new) + registration: a `clawdius.disableAnimations` boolean that
  swaps the animated brand art for the static stills - the empty-editor letterpress (via a
  `clawdius-disable-animations` container class the CSS keys off, applied to the main and every auxiliary
  window) and the Control Center header mark (`clawd-dance.svg` -> static `clawd.svg`). Pure CSS swap, no
  reflow.
- `src/vs/workbench/contrib/clawdius/browser/control/claudeControlCenterEditor.ts`: the Control Center
  Skills tab now groups plugin-bundled skills under a collapsible per-plugin header (standalone skills
  still render flat). A plugin-bundled skill is read-only (Open, no Delete or on/off toggle) because the
  plugin owns it and the name-keyed override does not reliably apply to plugin skills.
- `src/vs/workbench/contrib/clawdius/browser/usage/media/claudeUsage.css`: usage hover-popup contrast fix -
  the capacity bar's low-contrast lavender fill made the fill level unreadable; it now uses a neutral-gray
  track under the brand-orange fill so the filled portion stays legible.
